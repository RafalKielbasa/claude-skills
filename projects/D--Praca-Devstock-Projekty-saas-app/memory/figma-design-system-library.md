---
name: figma-design-system-library
description: "Figma design-system library file for udu-saas — built via MCP from apps/web tokens; file key, conventions, build state"
metadata: 
  node_type: memory
  type: project
  originSessionId: c8dc6ed5-d383-473e-a0c4-69ae390c4fd8
  modified: 2026-09-04T09:20:28.021Z
---

A dedicated Figma library **"udu-saas Design System"** is being built via the Figma MCP (`figma-generate-library` + `figma-use` skills) from the `apps/web` design-system foundations (see [[design-system-foundations-work]]).

- **File key:** `zVmr90NcDZkrG6siiRIuot` — https://www.figma.com/design/zVmr90NcDZkrG6siiRIuot . Created in team `team::1246021536676938130` (Devstock pro, Full seat). Figma account = `devstockspzoo@gmail.com` (NOT the user's own email).
- **NOT** the same as the reference mockup file `udu-saas-v1` (`BwdwVKDnvYwjY835CdufJA`), which has no formal variables.
- **Tokens (done):** collections `Primitives` (15 vars, hidden scope), `Color` (19 semantic, modes **Dark default** + Light, aliased to primitives), `Radius` (sm/md/lg). Text styles: `Text/sm`, `Text/sm-medium`, `Text/xs`, `Text/xs-medium` (Roboto). All vars have scopes + WEB code syntax `var(--x)` matching `globals.css`.
- **Components (all built):** Button (Variant×Size, 18), Input (4 States), Badge, Label, Separator, Skeleton, Textarea, Checkbox, Switch, RadioGroup, Select, Avatar, Progress, Card, Alert, Tabs, Tooltip, Dialog, Dropdown Menu. One page per component, each with dark canvas + token bindings. Overlays (Select/Tooltip/Dialog/Dropdown) built as static open-state representations.
- **NOT built (deliberately):** Sonner (runtime toast) and Form (react-hook-form wrapper) from the atoms spec — not meaningful as static Figma components.
- **Cover + Getting Started pages:** filled (hero cover with sample instances; Getting Started doc covering structure/tokens/conventions/usage/code mapping).
- **App screens (started):** assembling product screens IN THE SAME FILE from local component instances (skill `figma-generate-design`). Components/variables are LOCAL — use `getNodeByIdAsync(variantId).createInstance()` + `setProperties()` / direct text override, and `getVariableByIdAsync` for bindings (NOT importByKey/search_design_system — those are for remote libraries). Pages live after a `─── Screens ───` divider.
  - **DONE: CP-21 Student screens (katalog + szczegóły + odtwarzacz + stany brzegowe).** Top-bar shell for catalog/detail; 2-column shell (320px left lesson outline + main) for player. Page `45:8` **Katalog** now has 3 wrappers: `45:9` lista (REVISED — student top bar w/ outline "Moje kursy" instead of "+ Nowy kurs"; 6 custom 320px course cards = muted thumbnail+image-icon, title, meta, price/"Bezpłatny" Badge, "Zobacz kurs" — Card component lacks a thumbnail slot so cards are custom frames w/ tokens, NOT Card instances), `89:23` pusty (centered empty-state), `90:33` ładowanie (6 skeleton cards via Skeleton instances). Page `91:7` **Szczegóły kursu**: `91:8` niezapisany (cover banner, Badges, title, author Avatar, custom Tabs Opis/Program/Opinie, accordion sections w/ lesson rows = type icons video/article/quiz + "Bezpłatna"/lock; right enroll panel = price+strikethrough, "Kup kurs" lg full-width, guarantee, feature list w/ green checks), `95:15` zapisany (clone+edit: panel price→custom progress bar 35% + "Kontynuuj naukę"; lessons completed ✓/current "Teraz" highlight). Page `97:7` **Odtwarzacz** = 1 page, 7 wrappers via clone+edit-main: `97:8` Video (16:9 player frame plain-FRAME w/ absolute-positioned play button + control bar via constraints CENTER/STRETCH; lesson title/desc; footer Poprzednia/Oznacz ukończone/Następna), `101:11` Wideo przetwarzane (spinner+msg), `102:15` Artykuł (prose: h2/para/bullet list/indigo callout), `103:19` Quiz pytania (question cards w/ RadioGroup option rows + "Sprawdź odpowiedzi"), `105:23` Quiz wynik (green pass banner + per-option green/red feedback tints), `106:29` Treść płatna (locked player overlay + purchase bar), `106:246` Błąd ładowania (centered alert + retry). Left outline current-lesson highlight = secondary bg + 3px primary left border; moved per state.
  - DONE earlier: "Screen · Katalog kursów" original (page `45:8`) — superseded by the CP-21 revision above.
  - **DONE: CP-16 Dashboard twórcy** — 5 creator screens, all with a shared shell (left Sidebar 240px brand+nav Kursy/Subskrypcja/Profil; Topbar breadcrumb+Avatar; dark page bg ink). Pages: `59:7` Lista kursów (wrapper `59:8` — header + primary "Nowy kurs" + 5-row course list card with draft/published status pills + outline "Edytuj"); `64:7` Nowy kurs (wrapper `64:8` — form card: Label+Input/Textarea/Select, Cena field in Input State=Invalid + destructive error text, dashed thumbnail dropzone, footer Anuluj/Utwórz); `65:7` Edytor lekcji = ONE page with 3 wrappers built by clone+edit: `65:8` Video (custom tab bar Video/Artykuł/Quiz + "Przygotuj upload" + Mux Uploader dashed dropzone), `67:11` Artykuł (Tiptap toolbar B/I/U/H2/lists + sample body), `67:53` Quiz (placeholder empty state "Kreator quizów — wkrótce"). Spec: [[design-system-foundations-work]].
  - **DONE: Auth screens (CP-70/71/72)** — split-screen layout (left gradient brand panel indigo→ink + right form column ~380px on dark bg). `72:7` Logowanie (wrapper `72:8` — email/hasło Inputs, 401 error banner, primary submit, "lub" divider, custom Google button, register link); `76:7` Rejestracja = ONE page, 2 wrappers via clone: `76:8` uczeń (role toggle Uczeń/Twórca + email/hasło) and `78:12` twórca (extra Nazwa studia + Subdomena field with `.udu.app` suffix + format hint + 409 error banner). `80:7` Callback OAuth (wrapper `80:8` — centered spinner ring + "Logowanie…"). Error banners + Google button + role toggle + subdomain suffix are custom (no DS slot/token). Ticket text lived in `2026-06-18-frontend-auth-login-register-jira-tasks.md`, since deleted; tickets are GitHub issues, not Jira.
  - **DONE (2026-07-09): CP-30/31 payment screens** — 3 pages after Odtwarzacz: `240:7` **Twórca — Subskrypcja** (creator shell clone; wrappers `240:98` wybór planu A1 = 3 plan cards Basic 49/Pro 149 highlighted+Badge "Polecany"/Business 349 w/ check-icon SVG feature rows, `243:12` sukces A2 + green banner, `243:129` anulowano A3 + amber banner); `240:8` **Zakup kursu** (student top-bar clone; `244:42` kurs płatny B1 = centered 560px purchase card thumb+price+strikethrough+lg "Kup kurs"+Stripe lock note, `247:11` zapisano B2 + green banner, `247:213` płatność sukces B3 = /payments/success centered state, `247:262` anulowana B4 = /payments/cancel); `240:9` **Moje kursy** (`248:42` lista C1 = 3 course cards w/ "Zapisano <data>" + "Kontynuuj naukę", `248:142` pusty C2). Banner pattern reused from quiz result: fill green/amber 12%, stroke 50%, r10, pad 16/18. Green #5EC26A, amber ~#E6B84F.
  - **DONE (2026-07-09): Struktura kursu (CP-16)** — page `253:7` after Moje kursy; wrappers `253:15` szkic (A1: header = title + status pill Szkic amber(0.92,0.70,0.10)@15% r999 pad4/10 dot7 + outline "Edytuj dane" + primary "Publikuj"; section cards r5 border, header bg card pad12/16 w/ grip/pencil/trash SVG icons #6E7079 + sm-outline "+ Lekcja", lesson rows pad10/16 strokeTopWeight-only w/ type icons play/file/quiz #9CA0AA; dashed `dashPattern [6,6]` "+ Dodaj sekcję" row), `259:196` opublikowany (A2: pill green(0.133,0.773,0.369), primary swapped to outline "Cofnij publikację"), `259:389` pusty (A3: dashed empty-state card + primary "+ Dodaj sekcję").
  - **FIX (2026-07-09): creator shell Topbar** was FIXED w=100 + clipsContent (breadcrumb truncated, Avatar never visible) — set `layoutSizingHorizontal='FILL'` on Topbar + on its inner `spacer` across ALL creator wrappers (59:65, 64:20, 65:20, 67:23, 67:65, 240:110, 243:24, 243:141, 253:27). Watch for this when cloning old shells.
  - **DONE (2026-07-15): Profil (CP-82)** — page `296:7` "Screen · Profil" (last page); wrappers `296:23` A1 twórca wypełniony (creator shell clone from 240:98, Nav/Profil active = white 6% fill + Medium text; Form card 560px: Avatar 64 preview + URL input, Imię i nazwisko, Email disabled + hint, Separator, right-aligned primary "Zapisz zmiany"), `298:16` A2 twórca niekompletny + baner (inputs w placeholder state; Banner/Uzupełnij profil = primary var tint fill 12%/stroke 50%, r10, pad16, user SVG icon + texts + spacer + Button sm + X icon; shell height fix: content primaryAxisSizingMode AUTO + sidebar/main FILL), `301:25` A3 student (top-bar shell clone from 244:42, Body → auto-layout centered, banner + Page column 560 header+card clones). Gotcha hit: `field.findOne(n => n.type==='INSTANCE')` returns the LABEL instance, not Input — index children instead.
  - **DONE (2026-07-15): Rejestracja live-check (CP-81)** — 2 new wrappers on `76:7`: `304:21` twórca · subdomena zajęta (input value "jan", red indicator line under field, Error 409 banner removed), `304:294` twórca · subdomena dostępna (value "jan-akademia", green #5EC26A-ish indicator).
  - **FIX (2026-07-15):** Nowy kurs dropzone caption `64:121` → "PNG, JPG lub WebP · zalecane 1280×720 · maks. 5 MB" (aligned with CP-76/77 thumbnail upload tickets).
  - **DONE (2026-07-15): Analityka twórcy (CP-45)** — page `310:7` "Screen · Twórca — Analityka": `310:39` A1 dane (creator shell clone from profile 296:23; sidebar got a 4th nav item `Nav/Analityka` inserted after Kursy, active state; 3 KPI cards Kursanci/Zapisy/Przychód pad 20 gap 4; Courses table card = head row + 4 rows w/ strokeTopWeight 1, right-aligned numeric cells fixed 60/90, completion cell = 100x6 r999 track white 8% + primary-var fill width=pct + pct text), `312:46` A2 pusta (KPI zera + empty state card "Brak opublikowanych kursów" + primary sm CTA).
  - **DONE (2026-07-15): Plan-4 progress/gamification screens (CP-39/40)** — on page `91:7` new wrapper `306:23` "Szczegóły · zapisany, kurs ukończony + certyfikat" (progress 100%, green banner Kurs ukończony, button "Pobierz certyfikat (PDF)"); new page `307:7` "Screen · Leaderboard": `307:32` L1 lista (student top-bar clone from 95:16; centered 720 column; primary-tint "Twoja pozycja: #4" banner; card with 6 rows: rank badge circle 28 gold/silver/bronze @15% tint, Avatar 32 w/ initials, name, pts; "Ty" row white 6% fill; rows strokeTopWeight 1), `309:16` L2 pusty (trophy SVG + "Brak danych. Bądź pierwszy!" + outline sm CTA). **Tint gotcha:** `{ ...paint, opacity: 0.12 }` spread of a setBoundVariableForPaint result can silently keep opacity 1 — put `opacity` in the BASE paint object BEFORE calling setBoundVariableForPaint.
  - **DONE (2026-07-21): Podgląd lekcji — tryb podglądu twórcy (CP-?? / patrz Edytor lekcji).** New page `448:7` "Screen · Twórca — Podgląd lekcji", inserted right after Edytor lekcji (65:7). 2 wrappers CLONED from the student Odtwarzacz: `97:8` Video → `448:36` "Podgląd · Video (P1)", `102:15` Artykuł → `448:124` "Podgląd · Artykuł (P2)". Student **Top bar** removed and replaced by a full-width creator **Preview bar** (`449:15` / `451:15`, 1440×72): fill primary@16% + bottom stroke primary@50%; left cluster = eye SVG (`createNodeFromSvg`, strokes recolored to primary) + "Tryb podglądu" (fg) + "· Tak zobaczą tę lekcję kursanci" (muted-fg); right = Button **outline** instance "← Wróć do edycji". Student footer **Actions** frame dimmed to `opacity 0.4` (actions non-functional in preview). Entry point = the existing "Podgląd" button in the Edytor lekcji header (`65:107`). **Quiz preview skipped** (kreator quizów "wkrótce", Sprint 4). Two gotchas reconfirmed: (1) the Odtwarzacz wrapper is a **VERTICAL auto-layout** so `appendChild` lands the bar at the BOTTOM — use `wrapper.insertChild(0, bar)` + `layoutSizingHorizontal='FILL'` to pin it to the top; (2) `setBoundVariableForPaint` drops the paint's `opacity` back to 1 — bake tint opacity by cloning the `fills`/`strokes` array and setting `opacity` AFTER binding.
  - **DONE (2026-09-02): App shell — TopBar (spec 2026-09-02 app-shell, punkt 1 z 7).** Nowa strona `961:7` "Screen · Obudowa — Belka" (po Analityce). COMPONENT_SET **TopBar `963:396`**, 7 wariantów, właściwości `Kontekst` (Szkoła/Apex/Panel) × `Użytkownik` (Gość/Zalogowany) × `Wyszukiwarka` (Tak/Nie): `963:389` Szkoła/Zalogowany/Tak (Katalog aktywny, Moje kursy, Ranking, search 360 + Avatar), `963:390` Szkoła/Zalogowany/Nie (Moje kursy aktywne), `963:391` Szkoła/Gość/Tak (tylko Katalog + search + Button outline "Zaloguj się"), `963:392` Szkoła/Gość/Nie, `963:393` Apex/Gość (brand "udu", bez nav), `963:394` Apex/Zalogowany, `963:395` Panel (brand szkoły, okruszki "Panel twórcy › Kursy", avatar RK). Budowa: bar 1440×72 HORIZONTAL SPACE_BETWEEN pad 16/40, fill color/card, dolny stroke color/border@0.07; nav item = pad 8/12 r5, aktywny fill color/accent@0.06 + Medium 14 accent-foreground, nieaktywny Regular 14 muted-foreground@0.6. Placeholder nazwy szkoły "Kuźnia Kodu". Stare belki na ekranach (cloned per wrapper) NIE podmienione na instancje. Pozostałe punkty 2–7 (UserMenu, landing+/account, kurs z szyną, panel AppShell, ranking szkoły, teksty 301:25) czekają na review Rafała po każdym punkcie.
  - **DONE (2026-09-02): App shell — UserMenu (punkt 2 z 7).** Strona `961:7` przemianowana na "Screen · Obudowa (app shell)". COMPONENT_SET **UserMenu `966:111`** (x=1720, na prawo od TopBar), 4 warianty `Kontekst` × `Rola`: `966:107` Szkoła/właściciel lub admin (Profil, Moje konto, Panel twórcy, sep, label "Twoje szkoły", 3 wiersze szkół z check `color/primary` przy bieżącej + etykieta roli xs muted, sep, Wyloguj), `966:108` Szkoła/uczestnik (bez Panel twórcy, rola bieżącej = uczestnik), `966:109` Panel/właściciel lub admin ("Widok szkoły" zamiast "Panel twórcy", avatar RK), `966:110` Apex/dowolna (bez Moje konto, bez przełącznika, żadna szkoła nie zaznaczona). Budowa: root VERTICAL gap 8 counter MAX (Avatar `31:2` jako Trigger nad Content); Content 256 FIXED/HUG, fill color/popover, stroke color/border@0.07 INSIDE, r5, pad 4, gap 2; item pad 6/8 r3, hover = fill color/accent@0.06 + tekst accent-foreground (pokazane na "Profil"); teksty przez text styles Text/sm, Text/sm-medium, Text/xs. Decyzja Rafała 2026-09-02: panel w układzie AppShell ("sidebar do belki"), belka na całą szerokość, szyna pod nią.
  - **DONE (2026-09-02): TopBar podmieniony na instancje we WSZYSTKICH ekranach.** Uczeń (variant `963:390` bez wyszukiwarki, `963:389` z wyszukiwarką na katalogu): 45:9/89:23/90:33 (Katalog), 91:8/95:15/306:23 (Katalog), 7 wrapperów odtwarzacza 97:7 (Katalog; stara belka ze strzałką wstecz skasowana), 244:42/247:11 (Katalog), 247:213/247:262 (nic aktywne), 248:42/248:142 (Moje kursy), 307:32/309:16 (Ranking), 301:25 (nic). Aktywna pozycja = override w instancji (fill Nav/X + font/fill tekstu). Panel (variant `963:395`) PRZEBUDOWANY na AppShell w 15 wrapperach: 59:8, 64:8, 65:8/67:11/67:53, 240:98/243:12/243:129, 253:15/259:196/259:389, 296:23/298:16, 310:39/312:46 — wrapper VERTICAL [TopBar FILL, "Shell row" HORIZONTAL FILL/HUG [PanelRail (dawny Sidebar, bez Brand, FIXED 240/FILL), Main (bez Topbar, FILL/HUG)]]; drugi okruszek w instancji = stary tekst okruszka (np. "Kursy   ›   Nowy kurs" jako jeden tekst). POMINIĘTE celowo: 448:36/448:124 (podgląd twórcy ma własny Preview bar) i auth (bez belki wg specu). Nazwa "Sidebar" → "PanelRail" tylko w wrapperach; brak osobnego komponentu PanelRail.
  - **DONE (2026-09-03): geometria belki + komponent PanelRail.** Decyzja Rafała: belka ma trzy strefy, nic wyśrodkowane. Wszystkie 7 wariantów `TopBar 963:396` przestawione ze `SPACE_BETWEEN` na `primaryAxisAlignItems='MIN'`, `itemSpacing=0`; `Brand` `FIXED` 200 px (padding 40 + 200 = **strefa marki 240 px**, równa szerokości `PanelRail`) + `clipsContent`; tekst marki `layoutSizingHorizontal='FILL'` + `textAutoResize='HEIGHT'` + `maxLines=1` + `textTruncation='ENDING'`; slot `Nav`/`Breadcrumbs` `FILL` z `MIN`, więc nawigacja i okruszki startują dokładnie na 240 px. Apex (`963:393`, `963:394`) zostaje na `SPACE_BETWEEN`, bo ma tylko Brand + Right. Nowy COMPONENT_SET **`PanelRail 995:69`** na stronie `961:7` (HORIZONTAL, gap 40, pad 40): warianty `Aktywny` = Kursy `995:38` / Analityka `995:48` / Subskrypcja `995:58` / Profil `995:68`, plus **boolean property `Analityka#995:0`** (domyślnie `false`) spięta z `visible` pozycji `Nav/Analityka` — pozycja wchodzi dopiero z CP-45, więc przy wariancie `Aktywny=Analityka` trzeba ją włączyć ręcznie. **13 surowych ramek PanelRail podmienionych na instancje** na 6 stronach: 59:7 (1), 64:7 (1), 65:7 (3), 240:7 (3, Subskrypcja), 253:7 (3), 310:7 (2, Analityka + prop `true`); strona podglądu lekcji `448:7` nie ma szyny. Instancje: `FIXED` 240 + `layoutSizingVertical='FILL'` (wysokości 560/579/781 biorą się z rodzica). Stan aktywnej pozycji ujednolicony: aktywna = fill `VariableID:3:15` @6% + Roboto Medium + tekst `VariableID:3:14` @1; nieaktywna = bez fill + Regular + `3:14` @0.6 (część wierszy używała wcześniej `3:16` @1).
  - **DONE (2026-09-03): NavItem wyłuskany z szyny.** Nowy COMPONENT_SET **`NavItem 1000:29`** na `961:7`: warianty `Stan` = Aktywna `1000:22` (fill `VariableID:3:15` @6% + Roboto Medium + tekst `3:14` @1) / Nieaktywna `1000:25` (bez fill, Regular, `3:14` @0.6) / Wyłączona `1000:28` (Regular, `3:14` @0.3, odpowiednik `opacity-50` z `sidebar.tsx`), plus **text property `Etykieta#1000:0`** spięta z `characters`. Mistrz HUG; w szynie instancje dostają `layoutSizingHorizontal='FILL'`, w belce zostają HUG. Cztery warianty `PanelRail` przebudowane z instancji `NavItem` (Profil = Wyłączona wszędzie poza wariantem `Aktywny=Profil`), właściwość `Analityka#995:0` przepięta na nową instancję. **Cztery szkolne warianty `TopBar` też przebudowane** (`963:389`–`963:392`, 8 ramek `Nav/*` → instancje `NavItem`), stany mistrzów zachowane: 389 Katalog aktywny, 390 Moje kursy, 391 Katalog aktywny, 392 Katalog nieaktywny. Apex i Panel nie mają pozycji nawigacji, więc ich nie dotyczy.
  - **DONE (2026-09-03): aktywna pozycja belki to teraz właściwość, nie ręczne nadpisanie.** Dotąd 22 instancje `TopBar` trzymały aktywną pozycję jako nadpisany `fill` ramki plus krój i kolor tekstu; po podmianie na `NavItem` te nadpisania przestały istnieć i zostały odtworzone jako `setProperties({Stan})` na zagnieżdżonej instancji. Spis (na wypadek kolejnej migracji): **Katalog** 969:42/969:60/969:76 (katalog), 970:31/970:46/970:60 (szczegóły), 970:507/521/535/549/563/577/591 (odtwarzacz, 7), 970:605/970:619 (zakup); **Moje kursy** 970:661/970:675; **Ranking** 970:689/970:703; **żadna aktywna** 970:633/970:647 (płatność sukces i anulowanie) oraz 970:743 (profil studenta); bez nawigacji 970:718/970:731 (profil twórcy, wariant Panel). Kolejność w belce: Katalog, Moje kursy, Ranking.
  - **DONE (2026-09-03): nawigacja po kursie jako komponenty.** Trzy nowe zestawy na `961:7`: **`LessonRow 1013:928`** (9 wariantów `Stan` = Domyślna/Bieżąca/Ukończona × `Typ` = Wideo/Artykuł/Quiz, text properties `Tytuł#1013:0` i `Meta#1013:10`), **`SecHeader 1013:943`** (`Rozwinięta` = Tak `1013:935` / Nie `1013:942`, `Tytuł#1013:20`, `Meta#1013:23`), **`CourseRail 1013:1037`** (320 px: nagłówek z tytułem kursu i paskiem postępu + `Outline` z instancji SecHeader i LessonRow). Siedem paneli `Outline panel` na stronie odtwarzacza `97:7` podmienione na instancje `CourseRail` (1015:123 C1, 1015:209 C2, 1015:362 C3, 1015:423 C4, 1015:489 C5, 1015:260 D1, 1015:311 D2); bieżąca lekcja to teraz `setProperties({Stan})` na zagnieżdżonym wierszu. Układ per wrapper: C1/C2/D1/D2 bieżąca = Konfiguracja (1 ukończona), C3 bieżąca = JSX (2 ukończone), C4/C5 bieżąca = Quiz (3 ukończone).
  - **PUŁAPKA (2026-09-03): w szynie kursu ikona lekcji koduje STAN, nie typ.** Zielony okrąg ze znacznikiem `#5EC26A` = ukończona, okrąg z trójkątem `#7C82F4` = bieżąca, szara ikona `#6E7079` = typ lekcji (wideo/artykuł/quiz). Kolory są wpisane wprost, bez zmiennych. Zbudowałem najpierw `Typ` = Wideo/Artykuł/Quiz biorąc ikonę z pierwszego wiersza i wyszło, że „Wideo" to w rzeczywistości znacznik ukończenia; trzeba było skasować trzy komponenty i zbudować od nowa. Ikony typu dla wideo w szynie NIE MA (oba wiersze wideo są ukończone albo bieżące) — szary wariant powstał przez przefarbowanie ikony bieżącej na `#6E7079`.
  - **PUŁAPKA (2026-09-03): szewron sekcji rozwiniętej i zwiniętej różni się GEOMETRIĄ, nie obrotem.** Rozwinięta ma wektor 8×4 w punkcie (4,6), zwinięta 4×8 w (6,4); `rotation` obu wynosi 0. Test przez `node.rotation` niczego nie wykryje, trzeba porównać `width`/`height` wektora.
  - **PUŁAPKA (2026-09-03): `query()` nie przyjmuje ukośnika w wartości atrybutu.** `page.query('FRAME[name^=Nav/]')` wywala się na `Invalid selector: unexpected character '/'`. Obejście: `node.findAll(n => n.name.indexOf('Nav/') === 0)`.
  - **PUŁAPKA (2026-09-03): podmiana dzieci mistrza NIE przelicza układu w istniejących instancjach.** Po wymianie ramek `Nav/*` na instancje `NavItem` w wariantach `PanelRail` dwie instancje z nadpisaną widocznością (ekrany analityki) renderowały `Nav/Analityka` i `Nav/Subskrypcja` na tym samym `y=56`, czyli jedno na drugim. Pozostałe 11 instancji (bez nadpisań) przeliczyło się samo. Fix: `inst.setProperties({prop: false})` i zaraz `true` — toggle wymusza reflow. Po każdej operacji na dzieciach mistrza sprawdź `y` dzieci w instancjach, bo screenshot mistrza wygląda dobrze i nie pokazuje problemu.
  - **Screen gotcha:** `figma.createAutoLayout`/`createFrame` default to a WHITE fill — explicitly `node.fills=[]` on every transparent container, or dark tokens won't show. And `node.resize()` AFTER `layoutSizingHorizontal='FILL'` resets it to FIXED — set FILL last.
- **Gotcha reused on screens:** for a WRAP horizontal auto-layout the HEIGHT is the COUNTER axis — set `counterAxisSizingMode='AUTO'`; auto-layout component instances often need `layoutSizingVertical='HUG'` to expand to content.
- **Remaining DS work (separate from screens):** Code Connect mapping (after `@/components/ui/*` files exist), library publish (NOTE: variable/token sharing across files needs Org/Enterprise plan; team is Pro), QA audit (contrast/naming).

**Build conventions (important, learned the hard way):**
- **Spacing (decision 2026-07-09): Tailwind default scale ONLY** for gaps/paddings on screens — allowed px: 0,1,2,4,6,8,10,12,14,16,20,24,28,32,36,40,44,48,56,64,80,96…; no 18px/13px etc. No spacing variables in Figma or in the Tailwind config (rule documented in `apps/web/docs/design-system.md` → Spacing). All screen pages were fully snapped to the scale on 2026-07-09 (186 fixes: 3→2, 5→4, 7→6, 9→8, 11→10, 18→16, 52→48; component pages and instance internals untouched) — keep new work on-scale from the start.
- Component pages use a **dark canvas** (`page.backgrounds` = ink #101218) and component-set fill = transparent — otherwise dark-default tokens (light text, alpha overlays) are invisible on Figma's default light canvas.
- `node.resize()` **resets** `layoutSizing` to FIXED — set `HUG`/`FILL` AFTER resize, not before.
- `setSharedPluginData`/`setBoundVariableForPaint` work on scene nodes; **Variables have no `setSharedPluginData`**.
- State ledger on disk: `docs/superpowers/plans/.dsb-state-udu-ds.json` (gitignored dir) — has all collection/variable/page/component IDs for resuming.

Source spec: `docs/superpowers/specs/2026-06-05-design-system-atoms-design.md`. The junior task file `2026-06-05-design-system-atoms-jira-tasks-junior.md` no longer exists (checked 2026-09-04); tasks are GitHub issues, not Jira ([[feedback-tickets-are-github-issues]]).
