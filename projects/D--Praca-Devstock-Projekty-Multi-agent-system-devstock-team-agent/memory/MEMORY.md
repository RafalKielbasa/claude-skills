- [n8n expression gotchas](n8n-expression-gotchas.md) — $guid/$randomString return null (use $execution.id); Split Out keeps nesting; sub-workflow errors surface as AI Agent errors
- [n8n tags not settable via MCP](n8n-tags-not-settable-via-mcp.md) — no tag tool / no tags param / SDK has no tag primitive; use n8n UI, REST API, or direct workflows_tags DB insert
- [Read n8n workflows from Postgres](n8n-read-workflows-from-postgres.md) — dump full workflow JSON from devstock-team-agent-postgres-1 workflow_entity (json cols); only way to read availableInMCP:false workflows
- [n8n CLI import/publish](n8n-cli-import-publish.md) — deploy repo JSON → instance without UI: inject credentials + top-level id, stream via stdin (docker cp→/tmp no-ops), import:workflow, tags via DB, publish:workflow + container restart; n8n execute blocked while instance runs
- [n8n Postgres: jsonb arrays & target DB](n8n-postgres-jsonb-arrays-and-target-db.md) — resource mapper (Insert) odrzuca tablice JSON dla jsonb → executeQuery; dockerowa baza ≠ prod użytkownika (inny schemat id)
- [n8n drift diffing: defaults & publish model](n8n-drift-diffing-defaults-publish.md) — UI save strips default params (fake drift); production = workflow_history@activeVersionId, not draft; 14_github_create_issue is NEWER on server (webhook redesign) — don't overwrite from repo
- [Avoid gemini-2.5-flash-lite](gemini-avoid-flash-lite.md) — often unavailable despite GA; prefer gemini-2.5-flash/-pro; stable vs preview model list; found live in 01_process_meeting.json classifier
- [n8n SplitInBatches done-output & empty results](n8n-splitinbatches-done-output.md) — done emituje itemy odesłane do pętli (wzorzec z 07); 0 wyników = 0 itemów → gałąź „pusto" wymaga alwaysOutputData
- [n8n alwaysOutputData × error output](n8n-alwaysoutputdata-error-output.md) — z continueErrorOutput itemy lecą OBOMA wyjściami naraz (maskowanie + zwielokrotnione powroty pętli, spam Slack); fix: continueRegularOutput + IF na $json.error; error bywa stringiem
- [n8n CLI execute & debug przebiegów](n8n-cli-execute-and-exec-debug.md) — one-off `docker compose run --rm --no-deps n8n execute --id` (bez drugiego „n8n"; tylko executeWorkflowTrigger → tymczasowa kopia 99_smoke); execution_data dekodować biblioteką flatted; MSYS_NO_PATHCONV=1 dla /dev/stdin
