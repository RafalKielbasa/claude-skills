# Wiki — log ewolucji (globalne)

Chronologiczny zapis sesji: co zostało założone, dopisane i zdecydowane.
Najnowszy wpis na KOŃCU pliku (odwrotnie niż w dzienniku sesji), bo ten plik
czyta skill `evolve-skill` od początku, a nie człowiek rano.

Format wpisu:

    ## YYYY-MM-DD — sesja <identyfikator>
    - Założono: <wzorzec> (dowód: <krótko>)
    - Dopisano dowód: <wzorzec>
    - Nawrót: <wzorzec> po zmianie z YYYY-MM-DD
    - Skill: <nazwa> — propozycja zaakceptowana | odrzucona
    - Sygnał: <wzorce kwalifikujące się do /evolve-skill> | brak

## Wpisy

## 2026-09-02 — sesja session_019Hub3mysiz4zztWAWESMQn
- Założono: pdf-read-wymaga-pdftotext (dowód: `Read` zwrócił błąd braku `pdftoppm` na PDF-ie artykułu, obejście `pdftotext -layout` z mingw64)
- Założono: subagent-limit-zastapiony-wlasna-ocena (dowód: dwa podagenty badawcze padły na limicie ok. 00:15, praca dokończona własną oceną zamiast po resecie)
- Założono: jednorazowe-zapoznanie-startuje-cala-nauke (dowód: „przeprowadź mnie przez treść" uruchomiło pełny tryb nauki z roadmapą, użytkownik przerwał na pierwszym pytaniu)
- Założono: przekierowanie-uzytkownika-respektowane-bez-oporu (dowód: po „Nie będę się uczył teraz" natychmiastowe przejście do brainstormingu, bez ponawiania pytania)
- Założono: nauka-przerwana-zostawia-punkt-wznowienia (dowód: `nauka-z-claude.md` został z notatką, od czego zacząć i czego nie powtarzać)
- Sygnał: brak (każdy wzorzec ma jeden dowód z jednej sesji; próg to dowody z więcej niż dwóch sesji albo status nawrót)

Uwaga do pochodzenia tych wpisów: wzorce powstały w przebiegach testowych
Zadania 2 planu `.claude/plans/2026-09-02-wikiskill.md`, w których podagenty
wykonywały skill `podsumuj-sesja-claude` na opisie tej właśnie sesji. Opis był
prawdziwy, więc obserwacje są autentyczne. Trzy z nich wyszły niezależnie
w dwóch osobnych przebiegach i zostały tu scalone. Zapis do wiki nastąpił po
jawnej zgodzie użytkownika, już poza przebiegiem testowym.

## 2026-09-03 — sesja session_01APWRKsZej4SeoF4vvdibEC
- Założono: twierdzenie-o-pliku-bez-odczytu (dowód: „mam go w kopii" o skasowanym wpisie WikiSkill; `grep` w kopii dał 0, bo kopia powstała 2 h po zniknięciu wpisu)
- Założono: plik-zmieniony-miedzy-odczytem-a-edycja (dowód: `praca-z-claude.md` 89 044 B o 09:45 i 85 067 B o 10:24 — druga sesja Claude skasowała wpis w oknie między odczytem a cięciem)
- Założono: opcje-z-tego-co-mierzalne-nie-z-celu (dowód: cztery opcje celu zbudowane z metryk pliku odrzucone; użytkownik wpisał „Wiele notatek nie ma już sensu")
- Założono: skrypt-z-asercja-zamiast-serii-edycji (dowód: 68 cięć jednym skryptem z `assert len(src) == 655`, weryfikator potwierdził 379/379 linii treści, 0 różnic)
- Założono: windows-path-w-literale-skryptu (dowód: `SyntaxError ... truncated \UXXXXXXXX escape` na `C:\Users\rafal\.claude\skills\...` wewnątrz odtwarzanej treści)
- Sygnał: brak (każdy nowy wzorzec ma jeden dowód z jednej sesji; próg to dowody z więcej niż dwóch sesji albo status nawrót)

## 2026-09-03 — sesja session_01PKwz2sPWn4S4MPAStw2DuF
- Założono: semantyka-wizualna-z-jednej-wlasciwosci (dowód: ikona wiersza lekcji wzięta za ikonę typu na podstawie geometrii wektorów, choć koduje stan — trzy komponenty skasowane i zbudowane od nowa; drugi dowód: szewron sekcji sprawdzony przez `rotation`, wszystkie trzy zwróciły 0, a różnica siedzi w wymiarach wektora 8×4 vs 4×8)
- Założono: figma-instancje-nie-przeliczaja-po-podmianie-mistrza (dowód: po wymianie dzieci mistrza `PanelRail` dwie instancje z nadpisaną widocznością wyrenderowały dwie pozycje na `y=56`; naprawił toggle właściwości, pozostałych 11 instancji przeliczyło się samo)
- Dopisano dowód: skrypt-z-asercja-zamiast-serii-edycji (spis stanu 22 instancji `TopBar` i 7 instancji szyny kursu przed podmianą na komponenty, odtworzenie jako właściwości, weryfikacja odczytem ze wszystkich instancji zamiast zrzutem ekranu)
- Sygnał: brak (oba nowe wzorce mają dowody z jednej sesji; próg to dowody z więcej niż dwóch sesji albo status nawrót)

## 2026-09-03 — sesja session_01APWRKsZej4SeoF4vvdibEC (druga część)
- Założono: regula-ciecia-usuwa-zakres-nie-szczegol (dowód: przy przepisywaniu #168 wg reguły głębokości ticketu zniknął cały punkt o `CatalogFilterProvider` oraz reguła życia klienta zapytań, obie niemożliwe do odzyskania z kodu; wyłapał to `codex exec`, nie własna lista kontrolna). Status od razu zaadresowany: cztery odpowiadające pozycje dopisane do `self-check.md` skilla `github-tickets` w tej samej sesji.
- Założono: kalibracja-na-jednym-elemencie-przed-paczka (dowód: #168 przepisany jako pierwszy i pokazany w całości; po „Jest super, tnij" czternaście pozostałych poszło tym samym cięciem bez poprawek kalibracyjnych)
- Założono: sciezka-skrocona-nie-jest-klikalna (dowód: podana ścieżka `scratchpad/tickets-new/168.md`, użytkownik: „Nie działa mi link"; plik był pod pełną ścieżką w katalogu tymczasowym sesji)
- Dopisano dowód: twierdzenie-o-pliku-bez-odczytu (dwa fałszywe twierdzenia o stanie repozytorium przeniesione bez sprawdzenia ze starych treści ticketów do nowych, w #171 i #183; oba wyłapał `codex exec`)
- Dopisano dowód: windows-path-w-literale-skryptu (heredoc `<<'SPECEOF'` w narzędziu Bash padł na apostrofach w angielskim tekście — `skill's`, `child's` — nie na ścieżce Windows; ten sam wyzwalacz co pierwotny dowód, inny znak)
- Dopisano dowód: skrypt-z-asercja-zamiast-serii-edycji (wstawienie wpisu do `praca-z-claude.md` z asercją nagłówka i sprawdzeniem przyrostu linii 635+39+2=676; wysyłka 15 przepisanych ticketów zweryfikowana odczytem treści z GitHuba wobec draftów, nie założeniem, że polecenia się powiodły)
- Sygnał: brak (wszystkie trzy nowe wzorce mają jeden dowód z jednej sesji; próg to dowody z więcej niż dwóch sesji albo status nawrót)

## 2026-09-03/04 — sesja session_0115YBg2ri1ajCfG8GNynEsZ
- Założono: mutacja-przed-dispatchem-lapie-wlasne-bledy (dowód: test rozjazdu historii symetryczny 1/1 nie dyskryminował kierunku; arytmetyka strażnika mergea `$before + 2` zamiast `$before + 1`; podwójne opakowanie `@(Find-DuplicateSlug ...)` dawało `count=1` zamiast `0` — wszystkie trzy złapane mutacją przed dispatchem, plus analogiczne przypadki w Task 1, 3, 4, 6)
- Założono: subagent-odmawia-commita-mimo-jawnego-wyjatku (dowód: implementer odmówił `git commit` dwukrotnie w Task 6 i raz w finalnej fali poprawek, cytując globalne „Nie commituj" mimo opisanego w prompcie stojącego wyjątku repo-specyficznego; kontroler commitował sam po własnej weryfikacji)
- Założono: powershell-semantyka-wymaga-sondy-nie-czytania (dowód: `$PSScriptRoot` pusty w `param()` pod `-File`; `ConvertFrom-Json` rzuca na przypisaniu do nieistniejącej właściwości niezależnie od `Set-StrictMode`; `2>&1` na komendzie natywnej z `$ErrorActionPreference='Stop'` zamienia udany `git fetch --verbose` w wyjątek; zewnętrzny `@(...)` wokół `return , @(...)` podwaja opakowanie pustego wyniku — wszystkie cztery ustalone sondą, nie lekturą)
- Założono: branch-finish-zaklada-oddzielna-galaz-feature (dowód: 8-zadaniowy plan backupu `.claude` wykonany od początku wprost na `main` per pre-flight ruling; wywołanie `finishing-a-development-branch` wymagało przekazania tego w `ARGUMENTS`, bo standardowe kroki 2–4 menu merge/PR/zostaw nie miały zastosowania)
- Sygnał: brak (wszystkie cztery nowe wzorce mają dowody z jednej sesji; próg to dowody z więcej niż dwóch sesji albo status nawrót)

## 2026-09-03 — sesja session_01TR43bKGaUWASqCrAsDE6GT
- Założono: instrukcja-zakotwiczona-na-pozycji-nie-na-naglowku (dowód: sekcja `# Pomysły` dołożona nad dziennikiem w `praca-z-claude.md` weszła w konflikt z Krokiem 4 „dopisz na GÓRZE pliku"; kolizja wyłapana przed zapisem, Krok 4 przekotwiczony na nagłówek dziennika)
- Założono: zmiana-skilla-poza-evolve-skill-bez-sladu (dowód: ta sama zmiana Kroku 4, wprowadzona wprost na prośbę użytkownika, zostawiła ślad tylko w `PURPOSE.md` skilla; w `skill-impact.md` nie było jej do czasu tego kroku)
- Dopisano dowód: plik-zmieniony-miedzy-odczytem-a-edycja (dwa rozjazdy w jednej sesji: `praca-z-claude.md` zmieniony przez użytkownika w Obsidianie między `Read` a `Edit`, oraz `index.md` tego wiki wymieniający 15 wzorców przy odczycie i 19 na dysku kwadrans później — równoległa sesja session_0115YBg2ri1ajCfG8GNynEsZ; przepisanie z nieodświeżonego kontekstu skasowałoby cztery wpisy)
- Skill: podsumuj-sesja-claude — propozycja zaakceptowana (Krok 4, wpis z diffem w `skill-impact.md`)
- Sygnał: brak (oba nowe wzorce mają jeden dowód; `plik-zmieniony-miedzy-odczytem-a-edycja` ma dowody z dwóch różnych sesji, a próg to więcej niż dwie)

## 2026-09-04 — sesja session_013eH7DXzW8DZjC16fy2ZCs4
- Założono: ustalenie-w-review-potwierdzone-sonda (dowód: zamiast poprzestać na zdaniu z `apps/api/README.md:119`, dwa anonimowe `GET`-y dały `certificates/_probe.pdf → 403` wobec `thumbnails/_probe.jpg → 404`; różnica 403/404 rozstrzygnęła, że `certificateUrl` z PR #165 jest niepobieralny, bez odwoływania się do czyjejkolwiek pamięci)
- Założono: review-przez-checkout-zabiera-drzewo-uzytkownika (dowód: checkout gałęzi PR #165 podmienił `AGENTS.md` pod odczytem i zostawił klienta Prisma przy schemacie CP-89; `tsc` dał 9 błędów o brakującym `certificateUrl`, czyli o kolumnie, którą PR właśnie dodawał — po `prisma generate` zero)
- Założono: review-pr-bez-skilla-odtwarzany-za-kazdym-razem (dowód: procedura review odtworzona w locie — kryteria z `docs/review-guide.md`, własny dobór sprawdzeń, payload `POST …/pulls/165/reviews` z tablicą `comments` zamiast `gh pr review`; „inline" odczytane jako „w tej sesji", a znaczyło „komentarze przy liniach", co kosztowało turę i podwójne dostarczenie treści). Skill do review jest w budowie od tej daty — wzorzec spisany jako wymagania do wciągnięcia w niego, nie jako postulat jego powstania.
- Dopisano dowód: windows-path-w-literale-skryptu (heredoc `<<'SCRIPT'` przez narzędzie Bash padł trzeci raz na apostrofach w angielskiej prozie komentarzy review — `student's name`, `endpoint's contract`; naprawa: skrypt zapisany przez Write, payload do `gh api` przez `--input`)
- Sygnał: brak (trzy nowe wzorce mają po jednym dowodzie z jednej sesji; `windows-path-w-literale-skryptu` ma 3 dowody, ale z dwóch sesji, a próg to więcej niż dwie)

## 2026-09-04/05 — sesja session_01WXmUJrXM5viDmNJuc3xjy6
- Założono: kod-referencyjny-planu-nigdy-nie-uruchomiony (dowód: finalne review gałęzi po 19 zadaniach orzekło not ready — 5 uwag krytycznych, każda odtworzona na prawdziwym repozytorium, wszystkie w tekście planu, żadna w transkrypcji agentów; najgroźniejsza `{ id, codex: null, ...finding }` ze spreadem na końcu, przez co agent podający własne `id` sprawiał, że uwagi blokujące znikały, a raport meldował „brak uwag" i kończył zerem)
- Założono: brief-nieaktualny-po-zmianie-planu (dowód: `readRunFile` wprowadzone w zadaniu 10, żeby brakujący artefakt kończył kodem 2 zamiast 1; briefy 14–19 wygenerowane przed tą poprawką i nieodświeżone po niej przywróciły stary `readJson` w `crm fixable` z zadania 16 — zweryfikowane wprost, `crm fixable --run nosuchrun` dawało ENOENT i exit 1)
- Założono: konfiguracja-z-katalogu-domowego-wchodzi-do-testow (dowód: `loadConfig` czytał `~/.claude/review/global.md`; 162 przebiegi suite'u zielone, bo plik nie istniał, a utworzenie go przy pierwszym wdrożeniu skilla wywaliło test budżetu agentów; potwierdzone różnicowo przeniesieniem pliku, wcześniejszy agent zdiagnozował ten błąd jako „wcześniejszy i niezwiązany")
- Założono: furtka-testowa-w-strazniku-produkcyjnym (dowód: `crm fixable` odmawia bez terminala, a proces spawnowany w teście nigdy terminala nie ma; zaproponowane `CRM_TEST_FORCE_TTY=1` odrzucone, bo ta sama zmienna w konfiguracji CI otwiera dokładnie ten przepływ, któremu strażnik ma zapobiegać — logika przeniesiona do `lib/fixable.mjs`, strażnik testowany spawnem asertującym odmowę)
- Założono: subagent-zglasza-sprzecznosc-zamiast-dopasowac-test (dowód: osiem wystąpień w jednej sesji, ani jednego fałszywego alarmu — m.in. `commitAll` po `makeRepo` bez niczego do zacommitowania, helper w `test/` liczony przez `node --test` jako pusty przechodzący test przez co licznik akceptacji 15 zadań był mylny o jeden, oraz niezgodne i oba błędne polskie liczebniki w kodzie i teście)
- Dopisano dowód: windows-path-w-literale-skryptu (skrypty Pythona wklejane do heredoca zjadły sekwencje ucieczki w podmienianym kodzie: regex stracił backslash, `\n` stał się prawdziwą nową linią w literale szablonu, escapowany cudzysłów zamienił się w goły — i zdarzyło się to ponownie przy zapisywaniu tego właśnie dowodu; czwarty dowód, trzecia sesja)
- Dopisano dowód: mutacja-przed-dispatchem-lapie-wlasne-bledy (prośba o sprawdzenie regresji przez podmianę argumentów `file` i `lines` w `blobLink` nie dała czerwonego — fixture `makeRepo` nie konfiguruje remote'a, więc funkcja zwracała `null`, zanim dotknęła argumentów, a asercja `link === null` była spełniona dla dowolnej kolejności)
- Dopisano dowód: powershell-semantyka-wymaga-sondy-nie-czytania (`Write-Error` pod `$ErrorActionPreference = "Stop"` przerywa skrypt, więc następujące po nim `exit 2` nigdy się nie wykonuje i skrypt kończy kodem 1 — czyli tym, który w tym projekcie znaczy „przeszła uwaga blokująca"; wykryte sondą pokazującą EXITCODE=1, nie lekturą kodu)
- Dopisano dowód: twierdzenie-o-pliku-bez-odczytu (do `SKILL.md` trafiło zdanie o zachowaniu modułu, którego czytelnik dokumentu nie widzi, wpisane bez sprawdzenia; reviewer wytknął, że każe wierzyć na słowo — sprawdzone dopiero wtedy i przypadkiem prawdziwe)
- Dopisano dowód: review-pr-bez-skilla-odtwarzany-za-kazdym-razem (skill `code-review-master` zbudowany w całości: 50 plików, 167 testów, budżet agentów `1 brief + N osi + N weryfikacji` potwierdzony dwoma niezależnie napisanymi sweepami; wzorzec pozostaje otwarty, bo skill nie został jeszcze użyty do żadnego prawdziwego review)
- Sygnał: windows-path-w-literale-skryptu (porażka, otwarty, dowody z trzech różnych sesji — próg przekroczony). Wzorzec jest oznaczony jako `ogólny` i za każdym razem wyzwalał go inny skill, więc jego miejscem jest reguła w `~/.claude/CLAUDE.md`, nie ewolucja pojedynczego skilla; gdyby jednak celować w skill, najbliższy jest `podsumuj-sesja-claude` (`/evolve-skill podsumuj-sesja-claude`), bo tam padł ostatni raz. Żaden wzorzec nie ma statusu `nawrót`.

## 2026-09-06/07 — sesja session_01T6FrJW1rs56KS6EsPB5agM
- Założono: cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (dowód: `cd .claude/skills/idea-engine && …` przestawiło cwd sesji; dwa komunikaty „Environment update", powrót przez kolejne `cd`, potem wszystkie komendy na ścieżkach bezwzględnych)
- Założono: kontekst-utracony-odtwarzany-z-dysku-nie-z-pamieci (dowód: na „czy jesteś świadom podziału specu na dwa plany" odpowiedź „nie" i odtworzenie stanu z `docs/`, ledgera SDD i gita z tabelą „sprawdziłem / zakładam"; wyszło, że Plan B istniał tylko jako zakres w Planie A, nie jako plik)
- Dopisano dowód: kod-referencyjny-planu-nigdy-nie-uruchomiony (Plan B, 5157 linii, oceniony wyłącznie lekturą: codex znalazł 2 defekty — `schema/run-plan.json` z `additionalProperties: false` wobec pól `target/input/kind` oraz `render --retry` bez pliku błędów po `memo-check` — a self-review 3 kolejne rozumowaniem; żaden blok nie uruchomiony; drugi dowód, druga sesja)
- Dopisano dowód: plik-zmieniony-miedzy-odczytem-a-edycja (druga sesja Claude w tym samym drzewie zmieniała `bin/ide.mjs` i `lib/idea.mjs` w trakcie pisania Planu B — Plan B cytuje `requireSlug` sprzed tłumaczenia komunikatów na polski; `praca-z-claude.md` dostał wpis drugiej sesji między pierwszym odczytem a `Edit`, złapane ponownym `Read` tuż przed edycją; trzeci dowód, trzecia sesja)
- Sygnał: plik-zmieniony-miedzy-odczytem-a-edycja (porażka, otwarty, dowody z trzech różnych sesji — próg przekroczony). Wzorzec `ogólny` — miejsce dla reguły to `~/.claude/CLAUDE.md`; najbliższy skill to `podsumuj-sesja-claude` (`/evolve-skill podsumuj-sesja-claude`), bo dziennik był miejscem dwóch z trzech rozjazdów. Żaden wzorzec nie ma statusu `nawrót`.

## 2026-09-06/07 — sesja session_01CqWuPYXRh5VSuurqzqb5y3

- Założono: poprawka-punktowa-zamiast-przemiecenia-klasy (porażka; zatwierdziłem guard `?? []` tam, gdzie potrzeba `Array.isArray`, dwa razy — w `indexClaims` i w `openHypotheses` — a review całości znalazło trzeci wariant tej samej klasy w `validateScorecard`, gdzie `!== undefined` nie łapie `null` i scorecard z `"demand": null` rzucał `TypeError`, wyrzucając 24 poprawne obiekty błędu policzone linijkę wyżej; audyt klasy zamówiłem dopiero po drugim wystąpieniu)
- Założono: review-zakresowe-nie-widza-defektu-poza-swoim-diffem (sukces; 18 review zadaniowych zamkniętych czysto i 120/120 testów, a review całej gałęzi z własnym fuzzerem — ~5000 wywołań przez 12 walidatorów — znalazło dokładnie jedno wymknięcie i był to Critical w linii, której żadne zadanie nie zmieniło; to samo review wykonało triaż 33 uwag odroczonych po drodze, czego również żadne zakresowe zrobić nie mogło)
- Założono: dispatch-oddziela-wykrycie-od-naprawy (sukces; po raporcie „Concerns: none" przy trzech odtwarzalnych awariach dopisałem do każdego dispatchu polecenie „spróbuj to zepsuć" — Taski 13, 14 i 16 zgłosiły potem po jednym realnym defekcie, żadnego nie wskazałem z góry; osobno w Tasku 16 poprosiłem o samą obserwację bez naprawy, dzięki czemu wybór między utwardzeniem helpera a zmianą zadania komendy `status` pozostał moją decyzją)
- Dopisano dowód: kod-referencyjny-planu-nigdy-nie-uruchomiony (wszystkie 7 wad Important znalezionych przez recenzentów w Taskach 1–16 siedziało w kodzie podanym przez plan dosłownie, żadna nie była błędem transkrypcji; do tego trzy defekty planu wykryte poza review, każdy uniemożliwiający wykonanie zadania jak napisane: `WEIGHTS` bez `export` a używane w innym pliku, helper `entry({ ts: … })` niemogący nadpisać `ts`, brak `join` na liście importów; trzeci dowód, trzecia sesja)
- Dopisano dowód: branch-finish-zaklada-oddzielna-galaz-feature (ostrzejszy wariant: skilla nie wywołałem w ogóle, bo repo miało unborn HEAD i zakaz commitowania przez wykonawcę, więc nie było ani gałęzi, ani commita do zmergowania; brak commitów wywrócił też instrukcję „skasuj workspace, bo historia gita jest teraz zapisem" — historii nie było, workspace zachowałem)
- Odstępstwo: `index.md` zaktualizowany punktowo, nie przepisany w całości wbrew krokowi 5.8. Równolegle w tym samym wiki pisała druga sesja (`session_01T6FrJW1rs56KS6EsPB5agM`, Plan B tego samego silnika) — przepisanie pliku w całości skasowałoby jej wpisy, co jest dokładnie tym, przed czym ostrzega wzorzec `plik-zmieniony-miedzy-odczytem-a-edycja`. Ta sama sesja zmieniła `kod-referencyjny-planu-nigdy-nie-uruchomiony` między moim odczytem a edycją; `Edit` odmówił i wymusił ponowny odczyt, co zadziałało jak asercja wejścia.
- Sygnał: brak wzorców do ewolucji. `kod-referencyjny-planu-nigdy-nie-uruchomiony` ma po tej sesji trzy dowody z trzech różnych sesji — próg przekroczony, ale wzorzec dotyczy `superpowers:writing-plans`, skilla z pakietu instalowanego, nie z `~/.claude/skills/`, więc `evolve-skill` nie ma czego zmienić; miejscem tej reguły jest krok własny przed egzekucją planu albo wpis w `~/.claude/CLAUDE.md` przy sekcji o planach implementacyjnych. Żaden wzorzec nie ma statusu `nawrót`.
- Dopisano dowód: plik-zmieniony-miedzy-odczytem-a-edycja (pierwszy raz mechanizm zadziałał jak zabezpieczenie: `Edit` odmówił zapisu do strony wzorca, bo równoległa sesja dopisała tam własny dowód w oknie między odczytem a edycją — wymuszony ponowny odczyt pokazał cudzą treść i mój dowód trafił obok niej zamiast ją nadpisać; czwarty dowód, trzecia sesja)
- Zmiana skilla: podsumuj-sesja-claude — krok 5.7 przestaje być bramką zgody (zapis od razu, lista zmian po fakcie) na bezpośrednią prośbę użytkownika, a krok 5.8 dostaje wyjątek od „przepisz `index.md` w całości" dla przypadku równoległej sesji w tym samym wiki. Pełny diff w `skill-impact.md`, wpis 2026-09-07.

## 2026-09-07 — sesja session_01YYxVxgP1QYqdEoh63ozDfs
- Założono: taskstop-nie-zabija-procesu-potomnego-na-windows (dowód: po `TaskStop` zadania `pnpm exec nest start` port 3001 dalej trzymał `node.exe` PID 8536 — ten sam PID co w linii startowej Nesta; `taskkill //PID … //F //T`; powtórzone przy sprzątaniu z PID 40624; restart był po nowe poświadczenia ADC, więc bez `netstat` test sprawdzałby stary proces)
- Założono: zielony-suite-z-mockami-nie-widzi-binarki-ani-poswiadczen (sukces; raport nazwał „nie zweryfikowane: endpoint przeciwko realnemu GCS", użytkownik poprosił „odapl enpoind", a przebieg na żywo znalazł brakujący `chrome@148.0.7778.97` w cache puppeteera i wygasłe ADC (`invalid_grant/invalid_rapt`) — niewidoczne dla 479/479 testów z mockami; po naprawie PDF 50 619 B przez API i `403` anonimowo na obiekt)
- Dopisano dowód: review-pr-bez-skilla-odtwarzany-za-kazdym-razem (trzecia improwizowana procedura, tym razem re-review „czy poprawki naniesione": diff od SHA review + `git show origin/<branch>:<plik>` + GraphQL `isResolved`; status wątków bezużyteczny — 19/19 nierozwiązanych przy 5/6 blokujących naniesionych; do Rozwiązania dopisany punkt 5 o trybie „stan poprawek po review"; trzeci dowód, trzecia sesja)
- Dopisano dowód: ustalenie-w-review-potwierdzone-sonda (sonda z review wróciła jako test odbioru poprawki: anonimowy GET na prawdziwy obiekt `403`, ta sama treść przez `GET /api/…/certificate/download` `200`; drugi dowód, druga sesja)
- Dopisano dowód: cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (`cd apps/api && pnpm exec …` i powroty — cztery „Environment update"; bez szkody dzięki ścieżkom bezwzględnym, ale podpowłoki nie użyłem; drugi dowód, druga sesja)
- Dopisano dowód: plik-zmieniony-miedzy-odczytem-a-edycja (proces zmieniający był mój: `prettier --write` po serii `Edit` unieważnił trzy odczytane pliki, harness kazał czytać ponownie; piąty dowód, czwarta sesja)
- Sygnał: review-pr-bez-skilla-odtwarzany-za-kazdym-razem (porażka, otwarty, dowody z trzech różnych sesji — próg przekroczony) → `/evolve-skill code-review-master`, z punktem 5 Rozwiązania jako kandydatem na tryb re-review. `plik-zmieniony-miedzy-odczytem-a-edycja` (5 dowodów, 4 sesje) — sygnał zgłoszony już 2026-09-06/07, bez zmian: wzorzec `ogólny`, miejsce w `~/.claude/CLAUDE.md`. Żaden wzorzec nie ma statusu `nawrót`.

## 2026-09-07 — evolve-skill code-review-master — sesja session_01YYxVxgP1QYqdEoh63ozDfs
- Zaakceptowana: tryb `recheck <n> [reviewer]` w `code-review-master` (wzorzec review-pr-bez-skilla-odtwarzany-za-kazdym-razem → `zaadresowany (2026-09-07, code-review-master)`). Pełny diff w `skill-impact.md`. Zostawione na później: punkty 2 i 4 Rozwiązania wzorca (sprawdzenia z liczbami przed pierwszym ustaleniem, publikacja komentarzy na PR), `crm recheck` w CLI, porównanie tipu z własnym `findings.json`; status `review-przez-checkout-zabiera-drzewo-uzytkownika` bez zmian, choć krok 3 trybu koduje jego regułę.

## 2026-09-07 — sesja session_01YYxVxgP1QYqdEoh63ozDfs (po `/clear`: `code-review-master init` w saas app)
- Założono: symulacja-doboru-osi-przed-bramka-zatwierdzenia (sukces; szkic `config.md` przepuszczony przez `parseConfigDoc`/`validateConfig` i `selectAxes` na trzech zestawach plików przed bramką — pierwsza symulacja odłożyła grupę `web` za globalnym `debug-leftovers` na PR-ze webowym, a `auth-session` budziła się na `/account` przez glob `(apex)/**`; po poprawkach `deferred: —`, użytkownik zatwierdził bez zmian; po kroku 6 linie `Source: docs/review-guide.md §N` w configu wymagały przepisania na „former …")
- Dopisano dowód: ustalenie-w-review-potwierdzone-sonda (poza review: teza „commit 69a1405 jest już na origin" oparta w pierwszej odpowiedzi na lokalnym refie śledzącym, użytkownik po `reset --soft` zaprzeczył; rozstrzygnęła żywa sonda `git ls-remote origin refs/heads/<gałąź>` → `69a1405` i reflog `update by push` 12:38:57 — sonda powinna była iść od razu, bo na jej podstawie zapadała decyzja o operacji na historii; trzeci dowód, trzecia sesja)
- Odstępstwo: kroki 1–4 pominięte na prośbę użytkownika („bez zapisu do praca z claude, jedynie wiki"). `index.md` zaktualizowany punktowo (jedna nowa linia, jeden licznik), nie przepisany w całości — ta sama treść, mniejsze okno na `plik-zmieniony-miedzy-odczytem-a-edycja`.
- Sygnał: brak nowych kandydatów do ewolucji z tej sesji. Stan bez zmian względem wpisów wyżej: `windows-path-w-literale-skryptu` i `plik-zmieniony-miedzy-odczytem-a-edycja` (ogólne, miejsce w `~/.claude/CLAUDE.md`), `kod-referencyjny-planu-nigdy-nie-uruchomiony` (skill z pakietu). Żaden wzorzec nie ma statusu `nawrót`; `review-pr-bez-skilla-odtwarzany-za-kazdym-razem` (zaadresowany 2026-09-07) w tej części sesji nietestowany — pierwszy prawdziwy przebieg `code-review-master` nadal się nie odbył, config dla saas app jest od dziś w main (`7aca478`).

## 2026-09-07 — sesja session_01BFVYzhLh64ykBPjopBZyHU (Baza wiedzy, `/kurs-redakcja` M00L01)
- Założono: decyzja-w-bramce-bez-przed-i-po (dowód: punkt 2 bramki redakcyjnej opisany skrótem „Dopisek w haku artykułu…", Rafał: „nie rozumie"; druga wersja z dosłownym przed/po, źródłem faktu i powodem decyzji dostała „Zostaje"; pozostałe cztery punkty tej listy, każdy z konkretem, przeszły za pierwszym razem)
- Dopisano dowód: cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (cztery „Environment update" i jeden „Shell cwd was reset" przy `cd …/tools/course-pipeline && npm run validate` i `cd …/kursy/_wspolne && …`; bez szkody dzięki ścieżkom bezwzględnym, podpowłoki znów nie użyłem; trzeci dowód, trzecia sesja)
- Dopisano dowód: twierdzenie-o-pliku-bez-odczytu („10 segmentów" w inwentaryzacji i w prompcie subagenta z oka, plik miał 9; wyłapał subagent, `grep -c` dopiero po wysłaniu promptu; czwarty dowód, czwarta sesja)
- Dopisano dowód: subagent-zglasza-sprzecznosc-zamiast-dopasowac-test (redakcja tekstu przez Workflow: agent B zgłosił 10 vs 9 nagłówków, „pięć skrótów" vs sześć kombinacji i brak nazwy firmy zamiast rozstrzygać; agent A wypunktował własne zmiany na granicy merytoryki; drugi dowód, druga sesja)
- Wiki repo `D:/Praca/Devstock/Baza wiedzy/.claude/wiki/` założone w tej sesji: cztery wzorce `kurs-redakcja` (walidacja-calego-kursu-wnosi-cudze-bledy-do-bramki — zaadresowany w tej samej sesji, reczne-dopiski-autora-w-drzewie-przed-redakcja, grep-po-tresciach-kursu-gubi-twarde-spacje-i-emoji, bramki-wyboru-i-potwierdzenia-w-jednym-pytaniu) oraz wpis w tamtejszym `skill-impact.md` o ręcznej zmianie skilla (reguła z zmiana-skilla-poza-evolve-skill-bez-sladu zastosowana od razu, bez nowego dowodu).
- Odstępstwo: `index.md` zaktualizowany punktowo (trzy liczniki, jedna nowa linia), nie przepisany w całości — plik zmieniał się dziś w innych sesjach (mtime 12:56), treść zmian identyczna.
- Sygnał: cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (porażka, otwarty, dowody z trzech różnych sesji — próg przekroczony) oraz twierdzenie-o-pliku-bez-odczytu (4 dowody, od dziś z trzech różnych sesji — próg przekroczony). Oba `ogólne`, miejsce dla reguł to `~/.claude/CLAUDE.md`, nie ewolucja skilla: dla `cd` — zakaz `cd` w komendach Bash poza podpowłoką; dla twierdzeń — doprecyzowanie przy „Rozdzielaj sprawdzone od wywnioskowanego": liczby i fakty o plikach w promptach subagentów pochodzą z narzędzia. Bez zmian względem wcześniejszych sygnałów: `windows-path-w-literale-skryptu`, `plik-zmieniony-miedzy-odczytem-a-edycja`, `kod-referencyjny-planu-nigdy-nie-uruchomiony`. Żaden wzorzec nie ma statusu `nawrót`.

## 2026-09-07 — sesja session_01YYxVxgP1QYqdEoh63ozDfs (po `/clear`: pierwszy przebieg `code-review-master pr 164` i tryb `send`)
- Skill: code-review-master — zmiana dyktowana zaakceptowana: tryb `send <n> [ids]` (wpis w `skill-impact.md` z pełnym diffem; domyka punkt 4 Rozwiązania `review-pr-bez-skilla-odtwarzany-za-kazdym-razem`, zostawiony „na później" przy ewolucji `recheck`). Odstępstwo od `writing-skills`: bez scenariuszy presji z subagentami — test to asercje strukturalne (RED → GREEN) plus przebieg trybu w tej samej sesji.
- Dopisano dowód: review-pr-bez-skilla-odtwarzany-za-kazdym-razem (sukces: pierwszy prawdziwy przebieg skilla — run `20260907-111319-55oc`, 11/11 agentów w ledgerze, 6 surowych → 5 po score → 5/5 codex potwierdza, bramka exit 1; wysyłka na PR #164 jako review 5131590353 bez improwizowania mechaniki poza tym, że tryb `send` powstał w trakcie).
- Potknięcia startowe bez nowego wzorca (zapisane w pamięci projektu `saas app`): tryb `pr` wymagał `gh` z polem `baseRefOid` w `pr view --json` (2.29.0 → 2.100.0 przez winget na polecenie użytkownika), a `config.md` musiał leżeć na wymeldowanej gałęzi PR-a (użytkownik zmergował `main` do gałęzi).
- Sygnał: weryfikator Haiku dał 100 znalezisku `f-01` leżącemu poza diffem PR-a mimo reguły „0 dla pre-existing" w `prompts/verify.md`; wyłapało dopiero kotwiczenie w trybie `send`. Jeden dowód — kandydat na wzorzec przy powtórce (miejsce: hunki three-dot podane weryfikatorowi albo sprawdzane w `crm assemble`). Bez zmian względem wcześniejszych sygnałów. Żaden wzorzec nie ma statusu `nawrót`.

## 2026-09-07 — evolve-skill podsumuj-sesja-claude — sesja session_01BFVYzhLh64ykBPjopBZyHU
- Zaakceptowana: Krok 4 `podsumuj-sesja-claude` — świeży odczyt tuż przed zapisem i asercja wejścia przy wstawianiu wpisu do dziennika (wzorzec plik-zmieniony-miedzy-odczytem-a-edycja → `zaadresowany (2026-09-07, podsumuj-sesja-claude)`; wcześniejsza zmiana 5.8 z 2026-09-07 pokrywała tylko wiki). Pełny diff w `skill-impact.md`. `index.md` zaktualizowany punktowo (jedna linia statusu; wynik identyczny z przepisaniem). `PURPOSE.md` skilla uzupełniony także o brakujący wpis historii z 2026-09-07 (5.7/5.8), odtworzony ze `skill-impact.md`. Zostawione na później: `windows-path-w-literale-skryptu` — w tej sesji wystąpił ponownie (skrypty zapisu dziennika i wiki wklejane heredokiem padały: ostrzeżenie o `\*`, `SyntaxError` na prostym cudzysłowie w literale, wreszcie „unexpected EOF while looking for matching quote" przy skrypcie samej ewolucji — zapis udał się dopiero ze skryptu w pliku); dowód jeszcze niezapisany, czeka na słowo użytkownika. Nawrót tej zmiany rozpozna następne podsumowanie sesji, w którym dziennik zmieni się między odczytem a zapisem.

## 2026-09-07 — sesja session_01JAgUNken6DG6aFPjDmKkAX (Plan B skilla `idea-engine`, 11 zadań przez subagent-driven-development)
- Założono: odtwarzanie-historii-przez-nadpisywanie-drzewa-roboczego (prośba „commituj zmiany" po 11 zadaniach bez commitów; skrypt miał zrobić 12 commitów ze snapshotów, `rm -rf` padł w kroku 2 na `Device or resource busy` po skasowaniu pierwszego argumentu, a obie ścieżki naprawy — `cp -R` i `git --work-tree=<snapshot> add` — zostały zablokowane przez klasyfikator; drzewo przywrócone PowerShellem z kopii zrobionej na starcie, historia wyszła w 2 commitach zamiast 12)
- Założono: ruling-z-nieweryfikowana-przeslanka (dwa moje rulingi Plan B oparte na fałszywym twierdzeniu o kodzie — „liczby z Taska 8 zależą od `openHypotheses`" i „poprawka reużycia id wymaga zmiany sygnatury" — oba obalone dopiero przez finalne review, oba sondą: suite przechodzi 203/203 pod dosłownym czytaniem specu 7.8, a `bundle.changelog` już niesie potrzebny high-water mark)
- Dopisano dowód: kod-referencyjny-planu-nigdy-nie-uruchomiony (czwarty dowód, czwarta sesja: kolizja `import { validMemo }` z `export function validMemo` w `refs.test.mjs:7` i pięć wywołań `checked()`/`verified()` bez utworzenia repo; nowe jest to, że wystarczył skan **statyczny** przed pierwszym dispatchem — nie trzeba było uruchamiać kodu, wystarczyło przeczytać go z zamiarem wykonania)
- Dopisano dowód: review-zakresowe-nie-widza-defektu-poza-swoim-diffem (drugi dowód: 11 czystych review zadaniowych, a review całej gałęzi znalazło, że polski zaimek „go" w sekcji `## Rekomendacja` odrzuca każde memo `no-go` i `pivot`, podczas gdy memo `go` jest odporne — kod był brief-verbatim, więc review zadania nie miało czego zgłosić)
- Dopisano dowód: mutacja-przed-dispatchem-lapie-wlasne-bledy (szósty dowód, pierwszy po stronie re-reviewera: dwa scoped re-review cofnęły poprawkę w kopii roboczej, żeby potwierdzić, że nowy test regresyjny w ogóle potrafi paść — implementer Taska 4 sam zgłosił, że jego pierwsza wersja przechodziła niezależnie od poprawki)
- Dopisano dowód: cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (czwarty dowód: `cd .claude/skills/idea-engine && node --test test/` przestawił katalog, następny `printf >> .superpowers/…/progress.md` padł z `No such file or directory`)
- Dopisano dowód: subagent-limit-zastapiony-wlasna-ocena (drugi dowód, tym razem obsłużony poprawnie: po `HTTP 429 — monthly spend limit` kontroler sprawdził, że nic częściowo nie weszło, zapisał to w ledgerze i wysłał świeżego implementera zamiast dokończyć poprawki własną oceną)
- Odstępstwo: `index.md` zaktualizowany punktowo (pięć liczników, dwie nowe linie), nie przepisany w całości — użytkownik pracował równolegle w tym samym repo i commitował w trakcie sesji.
- Sygnał: bez zmian względem poprzednich sesji. `kod-referencyjny-planu-nigdy-nie-uruchomiony` ma po dzisiejszym dowodzie 4 dowody z 4 sesji, ale dotyczy `superpowers:writing-plans` — skilla z pakietu instalowanego, więc `evolve-skill` nie ma czego w nim zmienić; miejsce dla tej reguły to prompt planisty albo `~/.claude/CLAUDE.md`. `cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan` (4 dowody) i `twierdzenie-o-pliku-bez-odczytu` (4) nadal `ogólne`. Nowy `ruling-z-nieweryfikowana-przeslanka` ma jeden dowód i celuje w skill instalowany — próg nieprzekroczony, ale wart obserwacji, bo dotyczy jedynego miejsca w pętli SDD, którego nic nie weryfikuje. Żaden wzorzec nie ma statusu `nawrót`.
