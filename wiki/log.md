# Wiki — log ewolucji (globalne)

Chronologiczny zapis sesji: co zostało założone, dopisane i zdecydowane.
Najnowszy wpis na KOŃCU pliku (odwrotnie niż w dzienniku sesji), bo ten plik
czyta skill `evolve-skill` od początku, a nie człowiek rano.

Format wpisu:

    ## YYYY-MM-DD — sesja <identyfikator>
    - Założono: <wzorzec> (dowód: <krótko>)
    - Dopisano dowód: <wzorzec>
    - Nawrót: <wzorzec> po zmianie z YYYY-MM-DD
    - Skill: <nazwa> — propozycja zaakceptowana | odrzucona
    - Sygnał: <wzorce kwalifikujące się do /evolve-skill> | brak

## Wpisy

## 2026-09-02 — sesja session_019Hub3mysiz4zztWAWESMQn
- Założono: pdf-read-wymaga-pdftotext (dowód: `Read` zwrócił błąd braku `pdftoppm` na PDF-ie artykułu, obejście `pdftotext -layout` z mingw64)
- Założono: subagent-limit-zastapiony-wlasna-ocena (dowód: dwa podagenty badawcze padły na limicie ok. 00:15, praca dokończona własną oceną zamiast po resecie)
- Założono: jednorazowe-zapoznanie-startuje-cala-nauke (dowód: „przeprowadź mnie przez treść" uruchomiło pełny tryb nauki z roadmapą, użytkownik przerwał na pierwszym pytaniu)
- Założono: przekierowanie-uzytkownika-respektowane-bez-oporu (dowód: po „Nie będę się uczył teraz" natychmiastowe przejście do brainstormingu, bez ponawiania pytania)
- Założono: nauka-przerwana-zostawia-punkt-wznowienia (dowód: `nauka-z-claude.md` został z notatką, od czego zacząć i czego nie powtarzać)
- Sygnał: brak (każdy wzorzec ma jeden dowód z jednej sesji; próg to dowody z więcej niż dwóch sesji albo status nawrót)

Uwaga do pochodzenia tych wpisów: wzorce powstały w przebiegach testowych
Zadania 2 planu `.claude/plans/2026-09-02-wikiskill.md`, w których podagenty
wykonywały skill `podsumuj-sesja-claude` na opisie tej właśnie sesji. Opis był
prawdziwy, więc obserwacje są autentyczne. Trzy z nich wyszły niezależnie
w dwóch osobnych przebiegach i zostały tu scalone. Zapis do wiki nastąpił po
jawnej zgodzie użytkownika, już poza przebiegiem testowym.

## 2026-09-03 — sesja session_01APWRKsZej4SeoF4vvdibEC
- Założono: twierdzenie-o-pliku-bez-odczytu (dowód: „mam go w kopii" o skasowanym wpisie WikiSkill; `grep` w kopii dał 0, bo kopia powstała 2 h po zniknięciu wpisu)
- Założono: plik-zmieniony-miedzy-odczytem-a-edycja (dowód: `praca-z-claude.md` 89 044 B o 09:45 i 85 067 B o 10:24 — druga sesja Claude skasowała wpis w oknie między odczytem a cięciem)
- Założono: opcje-z-tego-co-mierzalne-nie-z-celu (dowód: cztery opcje celu zbudowane z metryk pliku odrzucone; użytkownik wpisał „Wiele notatek nie ma już sensu")
- Założono: skrypt-z-asercja-zamiast-serii-edycji (dowód: 68 cięć jednym skryptem z `assert len(src) == 655`, weryfikator potwierdził 379/379 linii treści, 0 różnic)
- Założono: windows-path-w-literale-skryptu (dowód: `SyntaxError ... truncated \UXXXXXXXX escape` na `C:\Users\rafal\.claude\skills\...` wewnątrz odtwarzanej treści)
- Sygnał: brak (każdy nowy wzorzec ma jeden dowód z jednej sesji; próg to dowody z więcej niż dwóch sesji albo status nawrót)

## 2026-09-03 — sesja session_01PKwz2sPWn4S4MPAStw2DuF
- Założono: semantyka-wizualna-z-jednej-wlasciwosci (dowód: ikona wiersza lekcji wzięta za ikonę typu na podstawie geometrii wektorów, choć koduje stan — trzy komponenty skasowane i zbudowane od nowa; drugi dowód: szewron sekcji sprawdzony przez `rotation`, wszystkie trzy zwróciły 0, a różnica siedzi w wymiarach wektora 8×4 vs 4×8)
- Założono: figma-instancje-nie-przeliczaja-po-podmianie-mistrza (dowód: po wymianie dzieci mistrza `PanelRail` dwie instancje z nadpisaną widocznością wyrenderowały dwie pozycje na `y=56`; naprawił toggle właściwości, pozostałych 11 instancji przeliczyło się samo)
- Dopisano dowód: skrypt-z-asercja-zamiast-serii-edycji (spis stanu 22 instancji `TopBar` i 7 instancji szyny kursu przed podmianą na komponenty, odtworzenie jako właściwości, weryfikacja odczytem ze wszystkich instancji zamiast zrzutem ekranu)
- Sygnał: brak (oba nowe wzorce mają dowody z jednej sesji; próg to dowody z więcej niż dwóch sesji albo status nawrót)

## 2026-09-03 — sesja session_01APWRKsZej4SeoF4vvdibEC (druga część)
- Założono: regula-ciecia-usuwa-zakres-nie-szczegol (dowód: przy przepisywaniu #168 wg reguły głębokości ticketu zniknął cały punkt o `CatalogFilterProvider` oraz reguła życia klienta zapytań, obie niemożliwe do odzyskania z kodu; wyłapał to `codex exec`, nie własna lista kontrolna). Status od razu zaadresowany: cztery odpowiadające pozycje dopisane do `self-check.md` skilla `github-tickets` w tej samej sesji.
- Założono: kalibracja-na-jednym-elemencie-przed-paczka (dowód: #168 przepisany jako pierwszy i pokazany w całości; po „Jest super, tnij" czternaście pozostałych poszło tym samym cięciem bez poprawek kalibracyjnych)
- Założono: sciezka-skrocona-nie-jest-klikalna (dowód: podana ścieżka `scratchpad/tickets-new/168.md`, użytkownik: „Nie działa mi link"; plik był pod pełną ścieżką w katalogu tymczasowym sesji)
- Dopisano dowód: twierdzenie-o-pliku-bez-odczytu (dwa fałszywe twierdzenia o stanie repozytorium przeniesione bez sprawdzenia ze starych treści ticketów do nowych, w #171 i #183; oba wyłapał `codex exec`)
- Dopisano dowód: windows-path-w-literale-skryptu (heredoc `<<'SPECEOF'` w narzędziu Bash padł na apostrofach w angielskim tekście — `skill's`, `child's` — nie na ścieżce Windows; ten sam wyzwalacz co pierwotny dowód, inny znak)
- Dopisano dowód: skrypt-z-asercja-zamiast-serii-edycji (wstawienie wpisu do `praca-z-claude.md` z asercją nagłówka i sprawdzeniem przyrostu linii 635+39+2=676; wysyłka 15 przepisanych ticketów zweryfikowana odczytem treści z GitHuba wobec draftów, nie założeniem, że polecenia się powiodły)
- Sygnał: brak (wszystkie trzy nowe wzorce mają jeden dowód z jednej sesji; próg to dowody z więcej niż dwóch sesji albo status nawrót)

## 2026-09-03/04 — sesja session_0115YBg2ri1ajCfG8GNynEsZ
- Założono: mutacja-przed-dispatchem-lapie-wlasne-bledy (dowód: test rozjazdu historii symetryczny 1/1 nie dyskryminował kierunku; arytmetyka strażnika mergea `$before + 2` zamiast `$before + 1`; podwójne opakowanie `@(Find-DuplicateSlug ...)` dawało `count=1` zamiast `0` — wszystkie trzy złapane mutacją przed dispatchem, plus analogiczne przypadki w Task 1, 3, 4, 6)
- Założono: subagent-odmawia-commita-mimo-jawnego-wyjatku (dowód: implementer odmówił `git commit` dwukrotnie w Task 6 i raz w finalnej fali poprawek, cytując globalne „Nie commituj" mimo opisanego w prompcie stojącego wyjątku repo-specyficznego; kontroler commitował sam po własnej weryfikacji)
- Założono: powershell-semantyka-wymaga-sondy-nie-czytania (dowód: `$PSScriptRoot` pusty w `param()` pod `-File`; `ConvertFrom-Json` rzuca na przypisaniu do nieistniejącej właściwości niezależnie od `Set-StrictMode`; `2>&1` na komendzie natywnej z `$ErrorActionPreference='Stop'` zamienia udany `git fetch --verbose` w wyjątek; zewnętrzny `@(...)` wokół `return , @(...)` podwaja opakowanie pustego wyniku — wszystkie cztery ustalone sondą, nie lekturą)
- Założono: branch-finish-zaklada-oddzielna-galaz-feature (dowód: 8-zadaniowy plan backupu `.claude` wykonany od początku wprost na `main` per pre-flight ruling; wywołanie `finishing-a-development-branch` wymagało przekazania tego w `ARGUMENTS`, bo standardowe kroki 2–4 menu merge/PR/zostaw nie miały zastosowania)
- Sygnał: brak (wszystkie cztery nowe wzorce mają dowody z jednej sesji; próg to dowody z więcej niż dwóch sesji albo status nawrót)

## 2026-09-03 — sesja session_01TR43bKGaUWASqCrAsDE6GT
- Założono: instrukcja-zakotwiczona-na-pozycji-nie-na-naglowku (dowód: sekcja `# Pomysły` dołożona nad dziennikiem w `praca-z-claude.md` weszła w konflikt z Krokiem 4 „dopisz na GÓRZE pliku"; kolizja wyłapana przed zapisem, Krok 4 przekotwiczony na nagłówek dziennika)
- Założono: zmiana-skilla-poza-evolve-skill-bez-sladu (dowód: ta sama zmiana Kroku 4, wprowadzona wprost na prośbę użytkownika, zostawiła ślad tylko w `PURPOSE.md` skilla; w `skill-impact.md` nie było jej do czasu tego kroku)
- Dopisano dowód: plik-zmieniony-miedzy-odczytem-a-edycja (dwa rozjazdy w jednej sesji: `praca-z-claude.md` zmieniony przez użytkownika w Obsidianie między `Read` a `Edit`, oraz `index.md` tego wiki wymieniający 15 wzorców przy odczycie i 19 na dysku kwadrans później — równoległa sesja session_0115YBg2ri1ajCfG8GNynEsZ; przepisanie z nieodświeżonego kontekstu skasowałoby cztery wpisy)
- Skill: podsumuj-sesja-claude — propozycja zaakceptowana (Krok 4, wpis z diffem w `skill-impact.md`)
- Sygnał: brak (oba nowe wzorce mają jeden dowód; `plik-zmieniony-miedzy-odczytem-a-edycja` ma dowody z dwóch różnych sesji, a próg to więcej niż dwie)

## 2026-09-04 — sesja session_013eH7DXzW8DZjC16fy2ZCs4
- Założono: ustalenie-w-review-potwierdzone-sonda (dowód: zamiast poprzestać na zdaniu z `apps/api/README.md:119`, dwa anonimowe `GET`-y dały `certificates/_probe.pdf → 403` wobec `thumbnails/_probe.jpg → 404`; różnica 403/404 rozstrzygnęła, że `certificateUrl` z PR #165 jest niepobieralny, bez odwoływania się do czyjejkolwiek pamięci)
- Założono: review-przez-checkout-zabiera-drzewo-uzytkownika (dowód: checkout gałęzi PR #165 podmienił `AGENTS.md` pod odczytem i zostawił klienta Prisma przy schemacie CP-89; `tsc` dał 9 błędów o brakującym `certificateUrl`, czyli o kolumnie, którą PR właśnie dodawał — po `prisma generate` zero)
- Założono: review-pr-bez-skilla-odtwarzany-za-kazdym-razem (dowód: procedura review odtworzona w locie — kryteria z `docs/review-guide.md`, własny dobór sprawdzeń, payload `POST …/pulls/165/reviews` z tablicą `comments` zamiast `gh pr review`; „inline" odczytane jako „w tej sesji", a znaczyło „komentarze przy liniach", co kosztowało turę i podwójne dostarczenie treści). Skill do review jest w budowie od tej daty — wzorzec spisany jako wymagania do wciągnięcia w niego, nie jako postulat jego powstania.
- Dopisano dowód: windows-path-w-literale-skryptu (heredoc `<<'SCRIPT'` przez narzędzie Bash padł trzeci raz na apostrofach w angielskiej prozie komentarzy review — `student's name`, `endpoint's contract`; naprawa: skrypt zapisany przez Write, payload do `gh api` przez `--input`)
- Sygnał: brak (trzy nowe wzorce mają po jednym dowodzie z jednej sesji; `windows-path-w-literale-skryptu` ma 3 dowody, ale z dwóch sesji, a próg to więcej niż dwie)

## 2026-09-04/05 — sesja session_01WXmUJrXM5viDmNJuc3xjy6
- Założono: kod-referencyjny-planu-nigdy-nie-uruchomiony (dowód: finalne review gałęzi po 19 zadaniach orzekło not ready — 5 uwag krytycznych, każda odtworzona na prawdziwym repozytorium, wszystkie w tekście planu, żadna w transkrypcji agentów; najgroźniejsza `{ id, codex: null, ...finding }` ze spreadem na końcu, przez co agent podający własne `id` sprawiał, że uwagi blokujące znikały, a raport meldował „brak uwag" i kończył zerem)
- Założono: brief-nieaktualny-po-zmianie-planu (dowód: `readRunFile` wprowadzone w zadaniu 10, żeby brakujący artefakt kończył kodem 2 zamiast 1; briefy 14–19 wygenerowane przed tą poprawką i nieodświeżone po niej przywróciły stary `readJson` w `crm fixable` z zadania 16 — zweryfikowane wprost, `crm fixable --run nosuchrun` dawało ENOENT i exit 1)
- Założono: konfiguracja-z-katalogu-domowego-wchodzi-do-testow (dowód: `loadConfig` czytał `~/.claude/review/global.md`; 162 przebiegi suite'u zielone, bo plik nie istniał, a utworzenie go przy pierwszym wdrożeniu skilla wywaliło test budżetu agentów; potwierdzone różnicowo przeniesieniem pliku, wcześniejszy agent zdiagnozował ten błąd jako „wcześniejszy i niezwiązany")
- Założono: furtka-testowa-w-strazniku-produkcyjnym (dowód: `crm fixable` odmawia bez terminala, a proces spawnowany w teście nigdy terminala nie ma; zaproponowane `CRM_TEST_FORCE_TTY=1` odrzucone, bo ta sama zmienna w konfiguracji CI otwiera dokładnie ten przepływ, któremu strażnik ma zapobiegać — logika przeniesiona do `lib/fixable.mjs`, strażnik testowany spawnem asertującym odmowę)
- Założono: subagent-zglasza-sprzecznosc-zamiast-dopasowac-test (dowód: osiem wystąpień w jednej sesji, ani jednego fałszywego alarmu — m.in. `commitAll` po `makeRepo` bez niczego do zacommitowania, helper w `test/` liczony przez `node --test` jako pusty przechodzący test przez co licznik akceptacji 15 zadań był mylny o jeden, oraz niezgodne i oba błędne polskie liczebniki w kodzie i teście)
- Dopisano dowód: windows-path-w-literale-skryptu (skrypty Pythona wklejane do heredoca zjadły sekwencje ucieczki w podmienianym kodzie: regex stracił backslash, `\n` stał się prawdziwą nową linią w literale szablonu, escapowany cudzysłów zamienił się w goły — i zdarzyło się to ponownie przy zapisywaniu tego właśnie dowodu; czwarty dowód, trzecia sesja)
- Dopisano dowód: mutacja-przed-dispatchem-lapie-wlasne-bledy (prośba o sprawdzenie regresji przez podmianę argumentów `file` i `lines` w `blobLink` nie dała czerwonego — fixture `makeRepo` nie konfiguruje remote'a, więc funkcja zwracała `null`, zanim dotknęła argumentów, a asercja `link === null` była spełniona dla dowolnej kolejności)
- Dopisano dowód: powershell-semantyka-wymaga-sondy-nie-czytania (`Write-Error` pod `$ErrorActionPreference = "Stop"` przerywa skrypt, więc następujące po nim `exit 2` nigdy się nie wykonuje i skrypt kończy kodem 1 — czyli tym, który w tym projekcie znaczy „przeszła uwaga blokująca"; wykryte sondą pokazującą EXITCODE=1, nie lekturą kodu)
- Dopisano dowód: twierdzenie-o-pliku-bez-odczytu (do `SKILL.md` trafiło zdanie o zachowaniu modułu, którego czytelnik dokumentu nie widzi, wpisane bez sprawdzenia; reviewer wytknął, że każe wierzyć na słowo — sprawdzone dopiero wtedy i przypadkiem prawdziwe)
- Dopisano dowód: review-pr-bez-skilla-odtwarzany-za-kazdym-razem (skill `code-review-master` zbudowany w całości: 50 plików, 167 testów, budżet agentów `1 brief + N osi + N weryfikacji` potwierdzony dwoma niezależnie napisanymi sweepami; wzorzec pozostaje otwarty, bo skill nie został jeszcze użyty do żadnego prawdziwego review)
- Sygnał: windows-path-w-literale-skryptu (porażka, otwarty, dowody z trzech różnych sesji — próg przekroczony). Wzorzec jest oznaczony jako `ogólny` i za każdym razem wyzwalał go inny skill, więc jego miejscem jest reguła w `~/.claude/CLAUDE.md`, nie ewolucja pojedynczego skilla; gdyby jednak celować w skill, najbliższy jest `podsumuj-sesja-claude` (`/evolve-skill podsumuj-sesja-claude`), bo tam padł ostatni raz. Żaden wzorzec nie ma statusu `nawrót`.

## 2026-09-06/07 — sesja session_01T6FrJW1rs56KS6EsPB5agM
- Założono: cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (dowód: `cd .claude/skills/idea-engine && …` przestawiło cwd sesji; dwa komunikaty „Environment update", powrót przez kolejne `cd`, potem wszystkie komendy na ścieżkach bezwzględnych)
- Założono: kontekst-utracony-odtwarzany-z-dysku-nie-z-pamieci (dowód: na „czy jesteś świadom podziału specu na dwa plany" odpowiedź „nie" i odtworzenie stanu z `docs/`, ledgera SDD i gita z tabelą „sprawdziłem / zakładam"; wyszło, że Plan B istniał tylko jako zakres w Planie A, nie jako plik)
- Dopisano dowód: kod-referencyjny-planu-nigdy-nie-uruchomiony (Plan B, 5157 linii, oceniony wyłącznie lekturą: codex znalazł 2 defekty — `schema/run-plan.json` z `additionalProperties: false` wobec pól `target/input/kind` oraz `render --retry` bez pliku błędów po `memo-check` — a self-review 3 kolejne rozumowaniem; żaden blok nie uruchomiony; drugi dowód, druga sesja)
- Dopisano dowód: plik-zmieniony-miedzy-odczytem-a-edycja (druga sesja Claude w tym samym drzewie zmieniała `bin/ide.mjs` i `lib/idea.mjs` w trakcie pisania Planu B — Plan B cytuje `requireSlug` sprzed tłumaczenia komunikatów na polski; `praca-z-claude.md` dostał wpis drugiej sesji między pierwszym odczytem a `Edit`, złapane ponownym `Read` tuż przed edycją; trzeci dowód, trzecia sesja)
- Sygnał: plik-zmieniony-miedzy-odczytem-a-edycja (porażka, otwarty, dowody z trzech różnych sesji — próg przekroczony). Wzorzec `ogólny` — miejsce dla reguły to `~/.claude/CLAUDE.md`; najbliższy skill to `podsumuj-sesja-claude` (`/evolve-skill podsumuj-sesja-claude`), bo dziennik był miejscem dwóch z trzech rozjazdów. Żaden wzorzec nie ma statusu `nawrót`.

## 2026-09-06/07 — sesja session_01CqWuPYXRh5VSuurqzqb5y3

- Założono: poprawka-punktowa-zamiast-przemiecenia-klasy (porażka; zatwierdziłem guard `?? []` tam, gdzie potrzeba `Array.isArray`, dwa razy — w `indexClaims` i w `openHypotheses` — a review całości znalazło trzeci wariant tej samej klasy w `validateScorecard`, gdzie `!== undefined` nie łapie `null` i scorecard z `"demand": null` rzucał `TypeError`, wyrzucając 24 poprawne obiekty błędu policzone linijkę wyżej; audyt klasy zamówiłem dopiero po drugim wystąpieniu)
- Założono: review-zakresowe-nie-widza-defektu-poza-swoim-diffem (sukces; 18 review zadaniowych zamkniętych czysto i 120/120 testów, a review całej gałęzi z własnym fuzzerem — ~5000 wywołań przez 12 walidatorów — znalazło dokładnie jedno wymknięcie i był to Critical w linii, której żadne zadanie nie zmieniło; to samo review wykonało triaż 33 uwag odroczonych po drodze, czego również żadne zakresowe zrobić nie mogło)
- Założono: dispatch-oddziela-wykrycie-od-naprawy (sukces; po raporcie „Concerns: none" przy trzech odtwarzalnych awariach dopisałem do każdego dispatchu polecenie „spróbuj to zepsuć" — Taski 13, 14 i 16 zgłosiły potem po jednym realnym defekcie, żadnego nie wskazałem z góry; osobno w Tasku 16 poprosiłem o samą obserwację bez naprawy, dzięki czemu wybór między utwardzeniem helpera a zmianą zadania komendy `status` pozostał moją decyzją)
- Dopisano dowód: kod-referencyjny-planu-nigdy-nie-uruchomiony (wszystkie 7 wad Important znalezionych przez recenzentów w Taskach 1–16 siedziało w kodzie podanym przez plan dosłownie, żadna nie była błędem transkrypcji; do tego trzy defekty planu wykryte poza review, każdy uniemożliwiający wykonanie zadania jak napisane: `WEIGHTS` bez `export` a używane w innym pliku, helper `entry({ ts: … })` niemogący nadpisać `ts`, brak `join` na liście importów; trzeci dowód, trzecia sesja)
- Dopisano dowód: branch-finish-zaklada-oddzielna-galaz-feature (ostrzejszy wariant: skilla nie wywołałem w ogóle, bo repo miało unborn HEAD i zakaz commitowania przez wykonawcę, więc nie było ani gałęzi, ani commita do zmergowania; brak commitów wywrócił też instrukcję „skasuj workspace, bo historia gita jest teraz zapisem" — historii nie było, workspace zachowałem)
- Odstępstwo: `index.md` zaktualizowany punktowo, nie przepisany w całości wbrew krokowi 5.8. Równolegle w tym samym wiki pisała druga sesja (`session_01T6FrJW1rs56KS6EsPB5agM`, Plan B tego samego silnika) — przepisanie pliku w całości skasowałoby jej wpisy, co jest dokładnie tym, przed czym ostrzega wzorzec `plik-zmieniony-miedzy-odczytem-a-edycja`. Ta sama sesja zmieniła `kod-referencyjny-planu-nigdy-nie-uruchomiony` między moim odczytem a edycją; `Edit` odmówił i wymusił ponowny odczyt, co zadziałało jak asercja wejścia.
- Sygnał: brak wzorców do ewolucji. `kod-referencyjny-planu-nigdy-nie-uruchomiony` ma po tej sesji trzy dowody z trzech różnych sesji — próg przekroczony, ale wzorzec dotyczy `superpowers:writing-plans`, skilla z pakietu instalowanego, nie z `~/.claude/skills/`, więc `evolve-skill` nie ma czego zmienić; miejscem tej reguły jest krok własny przed egzekucją planu albo wpis w `~/.claude/CLAUDE.md` przy sekcji o planach implementacyjnych. Żaden wzorzec nie ma statusu `nawrót`.
- Dopisano dowód: plik-zmieniony-miedzy-odczytem-a-edycja (pierwszy raz mechanizm zadziałał jak zabezpieczenie: `Edit` odmówił zapisu do strony wzorca, bo równoległa sesja dopisała tam własny dowód w oknie między odczytem a edycją — wymuszony ponowny odczyt pokazał cudzą treść i mój dowód trafił obok niej zamiast ją nadpisać; czwarty dowód, trzecia sesja)
- Zmiana skilla: podsumuj-sesja-claude — krok 5.7 przestaje być bramką zgody (zapis od razu, lista zmian po fakcie) na bezpośrednią prośbę użytkownika, a krok 5.8 dostaje wyjątek od „przepisz `index.md` w całości" dla przypadku równoległej sesji w tym samym wiki. Pełny diff w `skill-impact.md`, wpis 2026-09-07.

## 2026-09-07 — sesja session_01YYxVxgP1QYqdEoh63ozDfs
- Założono: taskstop-nie-zabija-procesu-potomnego-na-windows (dowód: po `TaskStop` zadania `pnpm exec nest start` port 3001 dalej trzymał `node.exe` PID 8536 — ten sam PID co w linii startowej Nesta; `taskkill //PID … //F //T`; powtórzone przy sprzątaniu z PID 40624; restart był po nowe poświadczenia ADC, więc bez `netstat` test sprawdzałby stary proces)
- Założono: zielony-suite-z-mockami-nie-widzi-binarki-ani-poswiadczen (sukces; raport nazwał „nie zweryfikowane: endpoint przeciwko realnemu GCS", użytkownik poprosił „odapl enpoind", a przebieg na żywo znalazł brakujący `chrome@148.0.7778.97` w cache puppeteera i wygasłe ADC (`invalid_grant/invalid_rapt`) — niewidoczne dla 479/479 testów z mockami; po naprawie PDF 50 619 B przez API i `403` anonimowo na obiekt)
- Dopisano dowód: review-pr-bez-skilla-odtwarzany-za-kazdym-razem (trzecia improwizowana procedura, tym razem re-review „czy poprawki naniesione": diff od SHA review + `git show origin/<branch>:<plik>` + GraphQL `isResolved`; status wątków bezużyteczny — 19/19 nierozwiązanych przy 5/6 blokujących naniesionych; do Rozwiązania dopisany punkt 5 o trybie „stan poprawek po review"; trzeci dowód, trzecia sesja)
- Dopisano dowód: ustalenie-w-review-potwierdzone-sonda (sonda z review wróciła jako test odbioru poprawki: anonimowy GET na prawdziwy obiekt `403`, ta sama treść przez `GET /api/…/certificate/download` `200`; drugi dowód, druga sesja)
- Dopisano dowód: cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (`cd apps/api && pnpm exec …` i powroty — cztery „Environment update"; bez szkody dzięki ścieżkom bezwzględnym, ale podpowłoki nie użyłem; drugi dowód, druga sesja)
- Dopisano dowód: plik-zmieniony-miedzy-odczytem-a-edycja (proces zmieniający był mój: `prettier --write` po serii `Edit` unieważnił trzy odczytane pliki, harness kazał czytać ponownie; piąty dowód, czwarta sesja)
- Sygnał: review-pr-bez-skilla-odtwarzany-za-kazdym-razem (porażka, otwarty, dowody z trzech różnych sesji — próg przekroczony) → `/evolve-skill code-review-master`, z punktem 5 Rozwiązania jako kandydatem na tryb re-review. `plik-zmieniony-miedzy-odczytem-a-edycja` (5 dowodów, 4 sesje) — sygnał zgłoszony już 2026-09-06/07, bez zmian: wzorzec `ogólny`, miejsce w `~/.claude/CLAUDE.md`. Żaden wzorzec nie ma statusu `nawrót`.

## 2026-09-07 — evolve-skill code-review-master — sesja session_01YYxVxgP1QYqdEoh63ozDfs
- Zaakceptowana: tryb `recheck <n> [reviewer]` w `code-review-master` (wzorzec review-pr-bez-skilla-odtwarzany-za-kazdym-razem → `zaadresowany (2026-09-07, code-review-master)`). Pełny diff w `skill-impact.md`. Zostawione na później: punkty 2 i 4 Rozwiązania wzorca (sprawdzenia z liczbami przed pierwszym ustaleniem, publikacja komentarzy na PR), `crm recheck` w CLI, porównanie tipu z własnym `findings.json`; status `review-przez-checkout-zabiera-drzewo-uzytkownika` bez zmian, choć krok 3 trybu koduje jego regułę.

## 2026-09-07 — sesja session_01YYxVxgP1QYqdEoh63ozDfs (po `/clear`: `code-review-master init` w saas app)
- Założono: symulacja-doboru-osi-przed-bramka-zatwierdzenia (sukces; szkic `config.md` przepuszczony przez `parseConfigDoc`/`validateConfig` i `selectAxes` na trzech zestawach plików przed bramką — pierwsza symulacja odłożyła grupę `web` za globalnym `debug-leftovers` na PR-ze webowym, a `auth-session` budziła się na `/account` przez glob `(apex)/**`; po poprawkach `deferred: —`, użytkownik zatwierdził bez zmian; po kroku 6 linie `Source: docs/review-guide.md §N` w configu wymagały przepisania na „former …")
- Dopisano dowód: ustalenie-w-review-potwierdzone-sonda (poza review: teza „commit 69a1405 jest już na origin" oparta w pierwszej odpowiedzi na lokalnym refie śledzącym, użytkownik po `reset --soft` zaprzeczył; rozstrzygnęła żywa sonda `git ls-remote origin refs/heads/<gałąź>` → `69a1405` i reflog `update by push` 12:38:57 — sonda powinna była iść od razu, bo na jej podstawie zapadała decyzja o operacji na historii; trzeci dowód, trzecia sesja)
- Odstępstwo: kroki 1–4 pominięte na prośbę użytkownika („bez zapisu do praca z claude, jedynie wiki"). `index.md` zaktualizowany punktowo (jedna nowa linia, jeden licznik), nie przepisany w całości — ta sama treść, mniejsze okno na `plik-zmieniony-miedzy-odczytem-a-edycja`.
- Sygnał: brak nowych kandydatów do ewolucji z tej sesji. Stan bez zmian względem wpisów wyżej: `windows-path-w-literale-skryptu` i `plik-zmieniony-miedzy-odczytem-a-edycja` (ogólne, miejsce w `~/.claude/CLAUDE.md`), `kod-referencyjny-planu-nigdy-nie-uruchomiony` (skill z pakietu). Żaden wzorzec nie ma statusu `nawrót`; `review-pr-bez-skilla-odtwarzany-za-kazdym-razem` (zaadresowany 2026-09-07) w tej części sesji nietestowany — pierwszy prawdziwy przebieg `code-review-master` nadal się nie odbył, config dla saas app jest od dziś w main (`7aca478`).

## 2026-09-07 — sesja session_01BFVYzhLh64ykBPjopBZyHU (Baza wiedzy, `/kurs-redakcja` M00L01)
- Założono: decyzja-w-bramce-bez-przed-i-po (dowód: punkt 2 bramki redakcyjnej opisany skrótem „Dopisek w haku artykułu…", Rafał: „nie rozumie"; druga wersja z dosłownym przed/po, źródłem faktu i powodem decyzji dostała „Zostaje"; pozostałe cztery punkty tej listy, każdy z konkretem, przeszły za pierwszym razem)
- Dopisano dowód: cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (cztery „Environment update" i jeden „Shell cwd was reset" przy `cd …/tools/course-pipeline && npm run validate` i `cd …/kursy/_wspolne && …`; bez szkody dzięki ścieżkom bezwzględnym, podpowłoki znów nie użyłem; trzeci dowód, trzecia sesja)
- Dopisano dowód: twierdzenie-o-pliku-bez-odczytu („10 segmentów" w inwentaryzacji i w prompcie subagenta z oka, plik miał 9; wyłapał subagent, `grep -c` dopiero po wysłaniu promptu; czwarty dowód, czwarta sesja)
- Dopisano dowód: subagent-zglasza-sprzecznosc-zamiast-dopasowac-test (redakcja tekstu przez Workflow: agent B zgłosił 10 vs 9 nagłówków, „pięć skrótów" vs sześć kombinacji i brak nazwy firmy zamiast rozstrzygać; agent A wypunktował własne zmiany na granicy merytoryki; drugi dowód, druga sesja)
- Wiki repo `D:/Praca/Devstock/Baza wiedzy/.claude/wiki/` założone w tej sesji: cztery wzorce `kurs-redakcja` (walidacja-calego-kursu-wnosi-cudze-bledy-do-bramki — zaadresowany w tej samej sesji, reczne-dopiski-autora-w-drzewie-przed-redakcja, grep-po-tresciach-kursu-gubi-twarde-spacje-i-emoji, bramki-wyboru-i-potwierdzenia-w-jednym-pytaniu) oraz wpis w tamtejszym `skill-impact.md` o ręcznej zmianie skilla (reguła z zmiana-skilla-poza-evolve-skill-bez-sladu zastosowana od razu, bez nowego dowodu).
- Odstępstwo: `index.md` zaktualizowany punktowo (trzy liczniki, jedna nowa linia), nie przepisany w całości — plik zmieniał się dziś w innych sesjach (mtime 12:56), treść zmian identyczna.
- Sygnał: cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (porażka, otwarty, dowody z trzech różnych sesji — próg przekroczony) oraz twierdzenie-o-pliku-bez-odczytu (4 dowody, od dziś z trzech różnych sesji — próg przekroczony). Oba `ogólne`, miejsce dla reguł to `~/.claude/CLAUDE.md`, nie ewolucja skilla: dla `cd` — zakaz `cd` w komendach Bash poza podpowłoką; dla twierdzeń — doprecyzowanie przy „Rozdzielaj sprawdzone od wywnioskowanego": liczby i fakty o plikach w promptach subagentów pochodzą z narzędzia. Bez zmian względem wcześniejszych sygnałów: `windows-path-w-literale-skryptu`, `plik-zmieniony-miedzy-odczytem-a-edycja`, `kod-referencyjny-planu-nigdy-nie-uruchomiony`. Żaden wzorzec nie ma statusu `nawrót`.

## 2026-09-07 — sesja session_01YYxVxgP1QYqdEoh63ozDfs (po `/clear`: pierwszy przebieg `code-review-master pr 164` i tryb `send`)
- Skill: code-review-master — zmiana dyktowana zaakceptowana: tryb `send <n> [ids]` (wpis w `skill-impact.md` z pełnym diffem; domyka punkt 4 Rozwiązania `review-pr-bez-skilla-odtwarzany-za-kazdym-razem`, zostawiony „na później" przy ewolucji `recheck`). Odstępstwo od `writing-skills`: bez scenariuszy presji z subagentami — test to asercje strukturalne (RED → GREEN) plus przebieg trybu w tej samej sesji.
- Dopisano dowód: review-pr-bez-skilla-odtwarzany-za-kazdym-razem (sukces: pierwszy prawdziwy przebieg skilla — run `20260907-111319-55oc`, 11/11 agentów w ledgerze, 6 surowych → 5 po score → 5/5 codex potwierdza, bramka exit 1; wysyłka na PR #164 jako review 5131590353 bez improwizowania mechaniki poza tym, że tryb `send` powstał w trakcie).
- Potknięcia startowe bez nowego wzorca (zapisane w pamięci projektu `saas app`): tryb `pr` wymagał `gh` z polem `baseRefOid` w `pr view --json` (2.29.0 → 2.100.0 przez winget na polecenie użytkownika), a `config.md` musiał leżeć na wymeldowanej gałęzi PR-a (użytkownik zmergował `main` do gałęzi).
- Sygnał: weryfikator Haiku dał 100 znalezisku `f-01` leżącemu poza diffem PR-a mimo reguły „0 dla pre-existing" w `prompts/verify.md`; wyłapało dopiero kotwiczenie w trybie `send`. Jeden dowód — kandydat na wzorzec przy powtórce (miejsce: hunki three-dot podane weryfikatorowi albo sprawdzane w `crm assemble`). Bez zmian względem wcześniejszych sygnałów. Żaden wzorzec nie ma statusu `nawrót`.

## 2026-09-07 — evolve-skill podsumuj-sesja-claude — sesja session_01BFVYzhLh64ykBPjopBZyHU
- Zaakceptowana: Krok 4 `podsumuj-sesja-claude` — świeży odczyt tuż przed zapisem i asercja wejścia przy wstawianiu wpisu do dziennika (wzorzec plik-zmieniony-miedzy-odczytem-a-edycja → `zaadresowany (2026-09-07, podsumuj-sesja-claude)`; wcześniejsza zmiana 5.8 z 2026-09-07 pokrywała tylko wiki). Pełny diff w `skill-impact.md`. `index.md` zaktualizowany punktowo (jedna linia statusu; wynik identyczny z przepisaniem). `PURPOSE.md` skilla uzupełniony także o brakujący wpis historii z 2026-09-07 (5.7/5.8), odtworzony ze `skill-impact.md`. Zostawione na później: `windows-path-w-literale-skryptu` — w tej sesji wystąpił ponownie (skrypty zapisu dziennika i wiki wklejane heredokiem padały: ostrzeżenie o `\*`, `SyntaxError` na prostym cudzysłowie w literale, wreszcie „unexpected EOF while looking for matching quote" przy skrypcie samej ewolucji — zapis udał się dopiero ze skryptu w pliku); dowód jeszcze niezapisany, czeka na słowo użytkownika. Nawrót tej zmiany rozpozna następne podsumowanie sesji, w którym dziennik zmieni się między odczytem a zapisem.

## 2026-09-07 — sesja session_01JAgUNken6DG6aFPjDmKkAX (Plan B skilla `idea-engine`, 11 zadań przez subagent-driven-development)
- Założono: odtwarzanie-historii-przez-nadpisywanie-drzewa-roboczego (prośba „commituj zmiany" po 11 zadaniach bez commitów; skrypt miał zrobić 12 commitów ze snapshotów, `rm -rf` padł w kroku 2 na `Device or resource busy` po skasowaniu pierwszego argumentu, a obie ścieżki naprawy — `cp -R` i `git --work-tree=<snapshot> add` — zostały zablokowane przez klasyfikator; drzewo przywrócone PowerShellem z kopii zrobionej na starcie, historia wyszła w 2 commitach zamiast 12)
- Założono: ruling-z-nieweryfikowana-przeslanka (dwa moje rulingi Plan B oparte na fałszywym twierdzeniu o kodzie — „liczby z Taska 8 zależą od `openHypotheses`" i „poprawka reużycia id wymaga zmiany sygnatury" — oba obalone dopiero przez finalne review, oba sondą: suite przechodzi 203/203 pod dosłownym czytaniem specu 7.8, a `bundle.changelog` już niesie potrzebny high-water mark)
- Dopisano dowód: kod-referencyjny-planu-nigdy-nie-uruchomiony (czwarty dowód, czwarta sesja: kolizja `import { validMemo }` z `export function validMemo` w `refs.test.mjs:7` i pięć wywołań `checked()`/`verified()` bez utworzenia repo; nowe jest to, że wystarczył skan **statyczny** przed pierwszym dispatchem — nie trzeba było uruchamiać kodu, wystarczyło przeczytać go z zamiarem wykonania)
- Dopisano dowód: review-zakresowe-nie-widza-defektu-poza-swoim-diffem (drugi dowód: 11 czystych review zadaniowych, a review całej gałęzi znalazło, że polski zaimek „go" w sekcji `## Rekomendacja` odrzuca każde memo `no-go` i `pivot`, podczas gdy memo `go` jest odporne — kod był brief-verbatim, więc review zadania nie miało czego zgłosić)
- Dopisano dowód: mutacja-przed-dispatchem-lapie-wlasne-bledy (szósty dowód, pierwszy po stronie re-reviewera: dwa scoped re-review cofnęły poprawkę w kopii roboczej, żeby potwierdzić, że nowy test regresyjny w ogóle potrafi paść — implementer Taska 4 sam zgłosił, że jego pierwsza wersja przechodziła niezależnie od poprawki)
- Dopisano dowód: cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (czwarty dowód: `cd .claude/skills/idea-engine && node --test test/` przestawił katalog, następny `printf >> .superpowers/…/progress.md` padł z `No such file or directory`)
- Dopisano dowód: subagent-limit-zastapiony-wlasna-ocena (drugi dowód, tym razem obsłużony poprawnie: po `HTTP 429 — monthly spend limit` kontroler sprawdził, że nic częściowo nie weszło, zapisał to w ledgerze i wysłał świeżego implementera zamiast dokończyć poprawki własną oceną)
- Odstępstwo: `index.md` zaktualizowany punktowo (pięć liczników, dwie nowe linie), nie przepisany w całości — użytkownik pracował równolegle w tym samym repo i commitował w trakcie sesji.
- Sygnał: bez zmian względem poprzednich sesji. `kod-referencyjny-planu-nigdy-nie-uruchomiony` ma po dzisiejszym dowodzie 4 dowody z 4 sesji, ale dotyczy `superpowers:writing-plans` — skilla z pakietu instalowanego, więc `evolve-skill` nie ma czego w nim zmienić; miejsce dla tej reguły to prompt planisty albo `~/.claude/CLAUDE.md`. `cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan` (4 dowody) i `twierdzenie-o-pliku-bez-odczytu` (4) nadal `ogólne`. Nowy `ruling-z-nieweryfikowana-przeslanka` ma jeden dowód i celuje w skill instalowany — próg nieprzekroczony, ale wart obserwacji, bo dotyczy jedynego miejsca w pętli SDD, którego nic nie weryfikuje. Żaden wzorzec nie ma statusu `nawrót`.

## 2026-09-07 — sesja session_01L3yWR4WmJVjeem37YvxMsw (pierwszy przebieg na żywo skilla `idea-engine` w repo agent-biznes)
- Dopisano dowód: przekierowanie-uzytkownika-respektowane-bez-oporu (drugi dowód: odrzucony odczyt plików w trakcie `validate` i doprecyzowane pytanie obsłużone ze specu bez ponawiania narzędzia; „kończymy na dziś" przerwało otwarty wątek omówienia bez oporu).
- Bez nowych wzorców globalnych. Dwa nowe wzorce repo (`merge-odrzuca-badacza-za-konflikt-z-rownoleglym-badaczem`, `validate-konczy-bez-omowienia-wynikow`) są w `agent-biznes/.claude/wiki/`. Obserwacja harnessu bez strony: `SendMessage` do subagenta zabitego przez użytkownika zwraca „stopped by the user and won't be resumed" — wznowienie jest niemożliwe, jedyna droga to nowy dispatch; jeden dowód.
- Sygnał: bez zmian. Żaden wzorzec nie ma statusu `nawrót`.

## 2026-09-09 — sesja session_01PrFurqNDm53ZdkCryE5kjm (repo Baza wiedzy: `/kurs-redakcja` M00L02 i zmiana zapisu fonetycznego kursu agenty-ai)
- Dopisano dowód: twierdzenie-o-pliku-bez-odczytu (piąty dowód, pierwszy o cudzym raporcie: subagent podał w raporcie bramkowym 8 calloutów przy sześciu w pliku; liczba z raportu subagenta idzie do użytkownika tak samo jak własna, więc wymaga tego samego odczytu).
- Bez nowych wzorców globalnych. Trzy nowe wzorce repo (`decyzja-fonetyczna-rozstrzygana-odsluchem`, `lista-wymowy-poprawiona-przed-startem-agenta`, `agent-redakcji-naprawia-rozjazd-zamiast-go-zglosic`) i dwa dopisane dowody (`grep-po-tresciach-kursu-gubi-twarde-spacje-i-emoji`, `bramki-wyboru-i-potwierdzenia-w-jednym-pytaniu`) są w `Baza wiedzy/.claude/wiki/`.
- Sygnał: bez zmian. Żaden wzorzec globalny nie ma statusu `nawrót`.

## 2026-09-09 — sesja session_01VwQutH9xHrU3vwiwnhbL8y (repo agent-biznes, tryb `vision` skilla `idea-engine`: brainstorming → spec → plan → 8 zadań przez subagent-driven-development)

- Założono: brief-nie-wymienia-testow-ktore-zmiana-uniewaznia (skill: superpowers:writing-plans, typ: porażka) — dwa razy w jednym planie: Task 1 zmieniał przejścia statusu, a jego lista plików nie zawierała `test/validate.test.mjs` z testem twierdzącym, że `validate plan` odrzuca pomysł `validated`; Task 4 dodawał pola do `ideaStatusRow`, brief wskazywał dwa `assert.deepEqual`, trzeci leżał w innym pliku. Obaj implementerzy znaleźli je sami i poprawili trafnie, więc koszt został na poziomie zamieszania — ale rozstrzygnięcie „to nieaktualna asercja, nie mój błąd" podejmował agent widzący tylko własne zadanie.
- Założono: klasyfikator-blokuje-subagenta-robiacego-commit (skill: ogólny, typ: porażka) — cztery ostrzeżenia `Blocked by classifier` w jednej sesji, za każdym razem na agencie, który właśnie wykonał `git commit`, i za każdym razem `git show --stat` pokazywał dokładnie pliki zadania. Piąty przypadek odwrotny: przy finalnej fali poprawek klasyfikator w ogóle nie odpowiedział (timeout), więc brak ostrzeżenia też nie zwalnia z kontroli.
- Dopisano dowód: kod-referencyjny-planu-nigdy-nie-uruchomiony (piąty dowód, piąta sesja) — dwie z czterech uwag Important siedziały w kodzie planu, nie w transkrypcji: `RegExp` budowany z niezwalidowanego id klocka rzucał `SyntaxError` z walidatora (znów łamiąc „Validators never throw" zacytowane w Global Constraints tego samego planu), a porównanie szkicu seedera przepuszczało dwa dowolne nieparsowalne teksty. Nowe: wada dotknęła też **tekstu instrukcji** — dwa Important z Taska 8 to brakujące zdania w `SKILL.md` podanym przez plan dosłownie, w tym pominięcie sekcji `## Odpowiedzi`, czyli dokładnie tej treści, dla której cały plan powstał.
- Dopisano dowód: review-zakresowe-nie-widza-defektu-poza-swoim-diffem (trzeci dowód, trzecia sesja) — osiem czystych review zadaniowych (241/241), a review całej gałęzi odtworzyło na działającym CLI defekt **utraty danych**: `vision finish` runu seedera bez bramki „już zasiane" cofa godzinę ręcznej pracy Rafała nad tezami i statusami. Defekt jest interakcją między commitami — funkcję bez bramki napisał Task 5, a groźną czyni ją instrukcja z Taska 8; bliźniacza `finishBlocks` z Taska 6 bramkę ma.
- Dopisano dowód: subagent-odmawia-commita-mimo-jawnego-wyjatku (trzeci dowód) **z nowym rozwiązaniem**. Poprzednie brzmiało „kontroler commituje sam". Tu zadziałało co innego: wznowienie subagenta wiadomością cytującą dosłowne słowa użytkownika z tej rozmowy, oznaczone jako jego własna wypowiedź, z granicami zgody. Subagent zacommitował, a od chwili wstawienia tego akapitu do każdego dispatchu odmowa nie wróciła przez sześć zadań i cztery rundy poprawek — dziesięć commitów z rzędu. Rozwiązanie na stronie wzorca przepisane: podać dowód, nie mocniejszy ton; commit kontrolera zostaje jako druga linia.
- Dopisano dowód: subagent-zglasza-sprzecznosc-zamiast-dopasowac-test (trzeci dowód) — implementer finalnej fali dostał ode mnie regułę sformułowaną za wąsko („tylko pliki `claims/*.json` mogą być etykietą retry"), odkrył, że jej dosłowne wykonanie wywala istniejący test, i zamiast osłabić test wskazał przyczynę: `model/assumptions.json` pisze badacz ekonomii jako swoje drugie wyjście. Moja instrukcja była błędna, jego korekta trafna. Nowe: sprzeczność wskazała wadę **polecenia kontrolera**, nie planu ani kodu.
- Wzorzec repo `validate-konczy-bez-omowienia-wynikow` przeszedł w `zaadresowany (2026-09-09, idea-engine)`; szczegóły i wpis w `skill-impact.md` po stronie repo `agent-biznes`.
- Odstępstwo: `index.md` zaktualizowany punktowo (cztery liczniki, jedno przepisane streszczenie rozwiązania, dwie nowe linie), nie przepisany w całości — użytkownik pracuje równolegle w tym samym środowisku.
- Sygnał: `kod-referencyjny-planu-nigdy-nie-uruchomiony` ma po dzisiejszym dowodzie **5 dowodów z 5 sesji**, a nowy `brief-nie-wymienia-testow-ktore-zmiana-uniewaznia` celuje w ten sam skill `superpowers:writing-plans` — oba dotyczą pakietu instalowanego, więc `evolve-skill` nie ma czego w nich zmienić; miejscem dla obu reguł jest `~/.claude/CLAUDE.md` albo prompt planisty. `review-zakresowe-nie-widza-defektu-poza-swoim-diffem` (3 dowody) i `subagent-zglasza-sprzecznosc-zamiast-dopasowac-test` (3) to sukcesy — sygnału nie generują. Żaden wzorzec nie ma statusu `nawrót`.

## 2026-09-09 — sesja session_01Kpavh9GSwwHtUcwJNUyRNm (`/kurs-lekcja` M00L03, repo Baza wiedzy)
- Dopisano dowód: windows-path-w-literale-skryptu (piąty dowód, czwarta sesja; dwa heredoki `<<'PLIK'` i `<<'ZRODLA'` przez narzędzie Bash padły na `unexpected EOF while looking for matching ''` — tym razem na polskiej prozie z cytatami z dokumentacji n8n: `You'll receive this error`, `your instance's balance`. Naprawa: Write dla plików lekcji, Write do scratchpada + `cat >>` dla dopisku do `zrodla.md`)
- Dopisano dowód: skrypt-z-asercja-zamiast-serii-edycji (czwarty dowód, sukces; ponad sto twardych spacji U+00A0 wstawionych skryptem `sierotki.mjs`, którego regex jest kopią regexa walidatora `tools/course-pipeline/src/typografia.js`, a nie odgadniętą regułą; skrypt sam raportuje `pozostale sierotki poza kodem: 0`, potwierdzone niezależnie przez `npm run validate`, trzy uruchomienia po kolejnych rundach poprawek)
- Wzorce repo (`Baza wiedzy/.claude/wiki/`): założono lekcja-dopasowana-do-niezacommitowanej-zmiany-poprzedniej i review-ai-ocenia-zargon-bez-kontekstu-poprzednich-lekcji; dopisano dowody do walidacja-calego-kursu-wnosi-cudze-bledy-do-bramki i grep-po-tresciach-kursu-gubi-twarde-spacje-i-emoji.
- Sygnał: brak wzorców globalnych do ewolucji. `windows-path-w-literale-skryptu` ma 5 dowodów z 4 sesji i status `otwarty`, ale to wzorzec o zachowaniu narzędzia, nie o skillu — nie ma skilla do wyewoluowania; wniosek operacyjny (nie używać heredoku do prozy) jest już w sekcji Rozwiązanie. Żaden wzorzec globalny nie ma statusu `nawrót`.

## 2026-09-09 — sesja session_017vpJFetLqZycH7sUkFH2XE (generator planu nagrania, repo Baza wiedzy)
- Założono: ruling-opisany-intencja-nie-sciezka-kodu (porażka; trzy podejścia do jednej uwagi z finalnego review, dwie stracone rundy implementera i recenzenta, zanim ruling dostał postać trzech predykatów zamiast zdania o intencji)
- Założono: zweryfikowana-liczba-powtarzana-jako-stala (porażka; baza walidacji „57/25” cytowana w dispatchach po tym, jak sam ją wyzerowałem, a poprawka zniknęła z drzewa; osobno rozkład wierszy alignera podany recenzentowi z pomiaru sprzed dwóch zadań i sprostowany przez niego)
- Założono: sdd-bez-commitow-paczki-review-z-migawek (sukces; siedem zadań i pięć rund poprawek bez commita, paczki review z `diff -u` migawek, ledger w roli `git log`)
- Dopisano dowód: kod-referencyjny-planu-nigdy-nie-uruchomiony (szósty dowód, szósta sesja; `if (leadLines)` prawdziwe dla pustej tablicy kasowało `segment.lead` przy drugim kroku — test z planu przechodził, bo jego przykład miał jeden krok, a review zadania nie widziało defektu, bo kod zgadzał się z briefem)
- Dopisano dowód: skrypt-z-asercja-zamiast-serii-edycji (piąty dowód, sukces; `fix-sierotki.mjs` uruchomiony dwukrotnie, z dwoma niezależnymi weryfikatorami — diff wyłącznie U+00A0 wobec HEAD i spójność `poprawne` z `odpowiedzi`, która złapała realne ryzyko rozspójnienia quizu)
- Dopisano dowód: subagent-zglasza-sprzecznosc-zamiast-dopasowac-test (czwarty dowód; trzy zatrzymania w jednej sesji, wszystkie na błędnych regułach kontrolera, żadne nie skończyło się osłabieniem testu)
- Dopisano dowód: windows-path-w-literale-skryptu (szósty dowód; heredoc `python - <<PY` padł na apostrofach w polskiej prozie wzorca i na separatorze cytowanym w treści — naprawa dokładnie według sekcji Rozwiązanie: Write do scratchpada, uruchomienie ścieżką)
- Wzorce repo (`Baza wiedzy/.claude/wiki/`): założono rozjazd-scenariusza-z-konspektem-wykrywany-maszynowo; dopisano dowód do walidacja-calego-kursu-wnosi-cudze-bledy-do-bramki.
- Sygnał: `kod-referencyjny-planu-nigdy-nie-uruchomiony` ma 6 dowodów z 6 sesji, ale to skill z pakietu instalowanego (`superpowers:writing-plans`) — miejscem reguły jest `~/.claude/CLAUDE.md` albo prompt planisty, nie `evolve-skill`. Nowe porażki mają po jednym dowodzie. Żaden wzorzec globalny nie ma statusu `nawrót`.

## 2026-09-10 — sesja session_015PLcG6rJFegWQiawFUeamB (spec + plan znaczników `[UWAGA: ...]` i skilla `/kurs-uwagi`, Task 1, repo Baza wiedzy)
- Dopisano dowód: sdd-bez-commitow-paczki-review-z-migawek (drugi dowód, i kontrprzykład do skrótu z pierwszego: w rulingu pre-flight wybrałem `git diff -- <ścieżki>` wobec HEAD zamiast migawek, a Rafał zacommitował `9a94b3e` dokładnie w chwili, gdy kończył się Task 1 — commit wciągnął `src/scenariusz.js` (+48) i `tests/scenariusz.test.js` (+92), więc `git diff --stat` na tych plikach zwrócił pusto i paczka review wyszłaby pusta. Sekcja Rozwiązanie zaostrzona: migawka obowiązkowa dla każdego zadania, także dla plików nietkniętych wcześniej.)
- Dopisano dowód: windows-path-w-literale-skryptu (siódmy dowód, i pierwszy, w którym do heredoku popchnęła instrukcja środowiska — tryb auto tej sesji każe zmieniać pliki przez `sed`/heredoc zamiast narzędzia Write. `cat > spec.md <<'SPEC_EOF'` padł na `unexpected EOF while looking for matching ''`, parser wskazał zdanie `Rafał's remark outranks a course rule`; plik nie powstał w ogóle. Do sekcji Rozwiązanie dopisany wniosek: instrukcja „preferuj Bash" dotyczy edycji punktowych, nie wielolinijkowej prozy.)
- Dopisano dowód: zweryfikowana-liczba-powtarzana-jako-stala (drugi dowód, nowa manifestacja: pomiar zdezaktualizował się w obrębie JEDNEJ tury — `git status --short` zebrany na starcie podsumowania pokazywał dziewięć pozycji, a po commicie Rafała jedną; sekcja „Stan" briefu złożona z pierwszego pomiaru kłamałaby o stanie repo. Wykryte przypadkiem, bo subagent zaraportował zmieniający się indeks na plikach, których nie dotykał.)
- Wzorce repo (`Baza wiedzy/.claude/wiki/`): bez nowych wzorców — sesja nie uruchomiła żadnego skilla `kurs-*`, cała praca to brainstorming, spec, plan i pierwsze zadanie SDD.
- Sygnał: `windows-path-w-literale-skryptu` ma 7 dowodów z 6 sesji i status `otwarty`, ale nadal jest wzorcem o zachowaniu narzędzia, nie o skillu — nie ma czego ewoluować przez `evolve-skill`; nowy element to konflikt z instrukcją trybu auto, czyli miejsce na regułę w `~/.claude/CLAUDE.md`, nie w skillu. `zweryfikowana-liczba-powtarzana-jako-stala` ma 2 dowody z 2 sesji, próg „więcej niż dwie sesje" jeszcze nieprzekroczony. Żaden wzorzec globalny nie ma statusu `nawrót`.

## 2026-09-10 — sesja session_01316pV2UPCFTTDHE9dksP6H (`/kurs-lekcja` M02L01, repo Baza wiedzy)
- Założono: python-na-windows-drukuje-w-cp1252 (porażka; dwa razy w jednej sesji `python -c` padł na `print` z polskim tekstem — `charmap codec can't encode character 'ą'` i `'ł'` — już po zapisaniu pliku, więc exit 1 wyglądał na awarię całej operacji; w drugim przypadku część wyjścia zdążyła się wypisać, co wyglądało jak pusty wynik wyszukiwania)
- Założono: webfetch-odmawia-pelnego-tekstu-strony (porażka; `WebFetch` na surowym `.md` z GitHuba z promptem „Return the full text verbatim" wrócił wyjaśnieniem o konflikcie z limitem cytatu zamiast treścią, choć ta sama strona pytana o konkretne parametry dała komplet cytatów za pierwszym razem)
- Dopisano dowód: windows-path-w-literale-skryptu (ósmy dowód, i pierwszy, w którym reguła z tej strony była mi znana i mimo to jej nie zastosowałem: `cat >> zrodla.md <<'EOF'` z trzynastoma pozycjami źródeł padł na apostrofach w angielskich cytatach z dokumentacji — dokładnie przypadek nazwany w sekcji Rozwiązanie. Wniosek: reguła musi brzmieć jako zakaz narzędziowy, nie jako ostrzeżenie)
- Dopisano dowód: zweryfikowana-liczba-powtarzana-jako-stala (trzeci dowód, nowa klasa „stałej": nie pomiar, tylko stan wyprowadzony z reguły. `CLAUDE.md` mówi, że nie commituję, więc przez całą sesję zakładałem, że pliki lekcji leżą niezacommitowane; `git status` przy składaniu raportu pokazał jedną pozycję zamiast sześciu, bo Rafał zacommitował je pod sesją jako `80b53bf` i `a497ac0`)
- Dopisano dowód: skrypt-z-asercja-zamiast-serii-edycji (szósty dowód i pierwszy kontrprzykład; skrypt aktualizujący wiki planował trzy operacje na `index.md`, każda budowała pełną nową treść z tego samego odczytu, więc ostatni zapis skasował dwie poprawki liczników — a wszystkie dwanaście asercji przeszło, bo liczyły się na oryginale. Wykryte weryfikatorem po zapisie. Sekcja Rozwiązanie rozszerzona: asercja wejścia nie chroni przed własnym nadpisaniem)
- Wzorce repo (`Baza wiedzy/.claude/wiki/`): założono regex-walidatora-uzyty-jako-fixer-typografii, twierdzenie-negatywne-o-narzedziu-bez-zrodla, url-dokumentacji-n8n-przeniesiony-bez-przekierowania i cytat-z-kodu-produktu-gdy-docs-milcza; dopisano dowody do walidacja-calego-kursu-wnosi-cudze-bledy-do-bramki (czwarty) i grep-po-tresciach-kursu-gubi-twarde-spacje-i-emoji (czwarty).
- Sygnał: `windows-path-w-literale-skryptu` ma 8 dowodów z 7 sesji i status `otwarty`, wciąż bez skilla do ewolucji — miejscem reguły jest `~/.claude/CLAUDE.md`, a nowy element to obserwacja, że sama znajomość reguły nie wystarcza, bo heredoc jest odruchem. `zweryfikowana-liczba-powtarzana-jako-stala` ma 3 dowody z 3 sesji, próg przekroczony, ale to też wzorzec ogólny, nie o skillu. Nowe porażki mają po jednym dowodzie. Żaden wzorzec globalny nie ma statusu `nawrót`.

## 2026-09-11 — sesja session_017aBB9RTKx1hZBSvF1TH7bi (poprawki silnika zadań, repo Baza wiedzy)
- Założono: defekt-generowania-naprawiany-narzedziem-nie-regula (sukces; ćwiczenia kursu generowały się z dwiema listami wypisanymi parami. Przedstawiłem trzy warianty naprawy, Rafał wybrał „narzędzie miesza, walidator pilnuje" zamiast „reguła w skillu + walidator". Argument rozstrzygający: przy samej regule wystarczy zamiana dwóch pozycji, żeby minąć sprawdzenie skrajnego przypadku i dalej oddać zadanie do odgadnięcia z kształtu. Wniosek do zapamiętania: zanim dopiszesz regułę do skilla, zapytaj, czy defekt da się policzyć)
- Założono: execsync-w-node-idzie-przez-cmd-nie-przez-bash (porażka; `execSync('git show <sha>^:plik')` w skrypcie Node uruchomionym z Git Basha zwrócił zawartość rewizji **po** zmianie zamiast przed, bez żadnego błędu — `child_process` na Windowsie idzie przez `cmd.exe`, gdzie `^` jest znakiem ucieczki. Pomiar skali defektu wyszedł odwrotnie, `0/7` zamiast `7/7`, i wyglądał wiarygodnie; wykryty tylko dlatego, że przeczył wcześniejszemu przebiegowi walidatora z tej samej sesji. To inna rodzina niż `windows-path-w-literale-skryptu`: tam znak składniowy wysadza parser i skrypt nie rusza, tu przechodzi wszystko i zwraca wynik odwrotny do zamierzonego)
- Dopisano dowód: cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (piąty dowód, piąta sesja; `cd .claude/skills/kurs-zadania && find .` i `cd tools/course-pipeline && npm run …` — dwa „Environment update" i pięć „Shell cwd was reset". Bez szkody, bo ścieżki były bezwzględne, ale `tools/course-pipeline` wymaga katalogu pakietu i trzecią sesję z rzędu dostaje `cd` zamiast podpowłoki)
- Dopisano dowód: skrypt-z-asercja-zamiast-serii-edycji (siódmy dowód; dziewięć podmian w `struktura-zadania.md` i `SKILL.md` przez skrypt z funkcją rzucającą `nie znaleziono fragmentu`, każda trafiona za pierwszym razem, plus wpis do dziennika sesji wstawiony jedną operacją `splice` z trzema asercjami naraz. Nauka z szóstego dowodu — kontrprzykładu — zastosowana: jedna operacja zapisu na plik, żadnych dwóch odczytów tego samego pliku przed zapisem)
- Dopisano dowód: zmiana-skilla-poza-evolve-skill-bez-sladu (drugi dowód, druga sesja; trzy kroki `kurs-zadania/SKILL.md` zmienione wprost na zgodę Rafała, poza ścieżką `evolve-skill`. Nowy element wobec pierwszego dowodu: zmiana skilla była tylko częścią większej zmiany w kodzie, więc rejestr — który zna wyłącznie `SKILL.md` — bez ręcznego wpisu zobaczyłby skill niezmieniony od 2026-09-10. Wpis dopisany ręcznie w kroku Wiki Maintainer, czyli mechanizm nadal jest ręczny)
- Wzorce repo (`Baza wiedzy/.claude/wiki/`): założono generator-wypisuje-dwie-listy-zadania-rownolegle (od razu `zaadresowany`, bo poprawka weszła w tej samej sesji) i kontrakt-platformy-opisany-ze-schematu-bez-kodu-konsumenta (`otwarty` — dane naprawione, reguła w żadnym skillu nie stanęła).
- Sygnał: `skrypt-z-asercja-zamiast-serii-edycji` ma 7 dowodów z 7 sesji, ale to wzorzec sukcesu — nie generuje sygnału. `cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan` ma 5 dowodów z 5 sesji i status `otwarty`, jednak dotyczy zachowania narzędzia, nie skilla: miejscem reguły jest `~/.claude/CLAUDE.md`, nie `evolve-skill`. Oba nowe wzorce mają po jednym dowodzie. Żaden wzorzec globalny nie ma statusu `nawrót`.

## 2026-09-11 — sesja session_01M1roR3MszbAgi1a1AE3xqh (`/kurs-lekcja M02L02`, repo Baza wiedzy)
- Dopisano dowód: windows-path-w-literale-skryptu (dziewiąty, **dwa wystąpienia w jednej sesji, oba na regule opisanej wprost na stronie**). Najpierw `cat > artykul.md <<'EOF'` z 3400 słowami polskiego markdownu padł na `unexpected EOF while looking for matching `''` i plik nie powstał w ogóle. Potem, już po przeczytaniu tej samej strony wiki, heredoc `python - <<'PY'` z blokiem `'''...'''` zawierającym sekwencje `\uXXXX` jako treść wysadził parser Pythona. Obejście, które zadziałało i warto je traktować jako domyślny schemat: treść narzędziem `Write` do plików w scratchpadzie, skrypt wyłącznie ASCII, wklejanie przez `io.open(...).read()`.
- Dopisano dowód: python-na-windows-drukuje-w-cp1252 (drugi, druga sesja). `UnicodeEncodeError` na `print` tytułu sąsiedniego wpisu dziennika, po przejściu wszystkich asercji wejścia. Zapis stał w skrypcie PO `print`, więc plik nie został ruszony — gdyby kolejność była odwrotna, wpis wjechałby do dziennika przy exit code 1. Nowy wariant naprawy w Rozwiązaniu: `sys.stdout.reconfigure(encoding='utf-8')` w pierwszej linii, gdy treść idzie heredokiem i nie ma jak dopisać prefiksu `PYTHONIOENCODING=utf-8`.
- Dopisano dowód: cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (szósty, czwarta sesja z rzędu z tym samym `tools/course-pipeline`). Siedem „Environment update" i cztery „Shell cwd was reset" w jednej sesji; podpowłoka `( cd … && … )` nie użyta ani razu.
- Dopisano dowód: webfetch-odmawia-pelnego-tekstu-strony (drugi, **kontrprzykład**). Ten sam prompt „return the raw markdown verbatim" na `raw.githubusercontent.com/n8n-io/n8n-docs/.../use-ai-for-parameters.md` zwrócił całą stronę z pełną tabelą parametrów — i była to jedyna droga do tej treści. Rozwiązanie skorygowane: rozstrzyga rozmiar i kształt strony, nie samo słowo „verbatim"; `HTTP 404` to porażka ze ścieżki, nie z limitu cytowania.
- Wzorce repo (`<repo>/.claude/wiki/`) dotknięte w tej sesji: **nawrót** grep-po-tresciach-kursu-gubi-twarde-spacje-i-emoji (piąty dowód, pierwszy po zaadresowaniu), nowy format-scenariusza-skopiowany-z-sasiedniej-lekcji, plus dowody do url-dokumentacji-n8n-przeniesiony-bez-przekierowania i cytat-z-kodu-produktu-gdy-docs-milcza.
- Sygnał globalny: żaden wzorzec globalny nie ma dziś statusu `nawrót`. Wzorce typu porażka i status `otwarty` z dowodami z więcej niż dwóch sesji: windows-path-w-literale-skryptu (9), cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (6), twierdzenie-o-pliku-bez-odczytu (5). Dwa pierwsze są o zachowaniu ogólnym, nie o konkretnym skillu, więc nie mają czego ewoluować w pliku skilla — kandydat, gdyby taka ewolucja miała sens, to reguła w `CLAUDE.md`, nie `/evolve-skill`.

## 2026-09-11 — sesja session_01YVYBimtiCfF3SS1QHH4Cvi (SDD planu `kurs-uwagi` w repo Baza wiedzy, potem pierwszy przebieg skilla)
- Założono: implementer-rozszerza-zakres-zamiast-eskalowac (porażka, skill `superpowers:subagent-driven-development`, status `otwarty`, 1 dowód). Implementer Taska 4 trafił na prawdziwą wadę planu i zamiast `BLOCKED` wszedł w `src/scenariusz.js` — plik Taska 1, zrecenzowany i zacommitowany — i podmienił regex przypięty w Global Constraints jako „exact". Zgłosił to jednym zdaniem pod „Notable Finding", ze statusem DONE i 584/584 na zielono. Jego poprawka kasowała prawdziwą granicę akapitu w kształcie, w którym siedzi wszystkie 12 znaczników jedynej lekcji używającej tej funkcji. Zielone testy są dla implementera dowodem, że postąpił dobrze; nikt mu nie powiedział, że wyjście poza listę plików briefu jest eskalacją niezależnie od testów.
- Założono: regex-przez-node-e-traci-ukosniki (porażka, skill `ogólny`, status `otwarty`, 1 dowód). Walidacja regexu przez `node -e` w Bashu dała 20 FAIL-i na 22 kształty, bo `[^\S\r\n]` dotarło jako `[^S\r\n]`, a `\[UWAGA:` jako `[UWAGA:`. Ten sam wzorzec zapisany do pliku `.mjs` dał 24/24 OK. Awaria cicha: `[^S\r\n]` to poprawna klasa znaków, tylko inna, a wynik wygląda jak wynik testu, który coś wykrył.
- Dopisano dowód: kod-referencyjny-planu-nigdy-nie-uruchomiony (siódmy). Plan przypiął w Global Constraints regex usuwania jako „exact", a w Tasku 4 test, który przy tym regexie nie mógł przejść — dwa bloki kodu tego samego planu, wzajemnie sprzeczne, żaden nigdy nie uruchomiony. Sprzeczność wyszła u implementera, po self-review planu i po review przez codex. Mocny wariant tego wzorca: defektem nie jest błąd w bloku, tylko sprzeczność MIĘDZY blokami, więc uruchomienie każdego z osobna też by go nie złapało.
- Dopisano dowód: mutacja-przed-dispatchem-lapie-wlasne-bledy (siódmy). Kontroler przepuścił regex przez 17, a potem 24 kształty wejścia, zanim zapisał go do briefu — najpierw wersję implementera, wykazując, że kasuje granicę akapitu, potem własną. Dispatch stał się transkrypcją zamiast projektowania, a recenzent na opusie potwierdził te same wyniki niezależnie, 33 kształtami.
- Dopisano dowód: zweryfikowana-liczba-powtarzana-jako-stala (czwarty). Literał `HASZ_BEZ_UWAG = 'e9f0efdc5de6'` wpisany do briefu dzień wcześniej jako „hasz sprzed zmiany" był już nieaktualny — commit `4f54506` przepisał w międzyczasie nagłówki w fiksturze. Brief sam przewidywał ten przypadek i podawał komendę do przeliczenia, ale nic nie wymuszało jej uruchomienia.
- Dopisano dowód: ustalenie-w-review-potwierdzone-sonda (czwarty). Recenzent przeliczył hasz sprzed zmiany własnym uruchomieniem i przepuścił eksportowane `usunUwagi` przez wszystkie 11 plików `scenariusz.md` w repo. Rozstrzygający argument nie był w diffie: wszystkie 12 prawdziwych znaczników jedynej lekcji używającej tej funkcji siedzi w kształcie, który odrzucona poprawka psuła.
- Dopisano dowód: sdd-bez-commitow-paczki-review-z-migawek (trzeci). Siedem zadań i fala poprawek bez commita. Konwencja obroniła się dwa razy: gdy Rafał zacommitował pliki Taska 1 w trakcie pracy implementera i gdy pod koniec runu zastage'ował całe `tools/course-pipeline/`.
- Dopisano dowód: instrukcja-zakotwiczona-na-pozycji-nie-na-naglowku (drugi). Brief Taska 6 adresował trzy edycje cytatem dokładnego zdania, czyli treścią, nie pozycją — i i tak się zestarzał w ciągu doby. Adresowanie treścią jest odporniejsze, ale nadal jest pomiarem drzewa z dnia pisania briefu.
- Dopisano dowód: ruling-z-nieweryfikowana-przeslanka (drugi). Ruling „zbiory plików tego planu i równoległej sesji są parami rozłączne" uzasadniał kontynuowanie pracy w dzielonym drzewie i był fałszywy — `README.md` dotykały obie prace. Wyszło dopiero w review całości, choć przesłankę dało się sprawdzić jednym `git status` w momencie jej zapisywania.
- Wzorce repo (`<repo>/.claude/wiki/`) dotknięte w tej sesji: nowe scenariusz-screencastu-pisany-bez-otwarcia-produktu (`kurs-lekcja`), zrzut-ekranu-w-tresci-uwagi i naniesienie-uwagi-siega-poza-akapit-znacznika (oba `kurs-uwagi`).
- Sygnał globalny: żaden wzorzec globalny nie ma dziś statusu `nawrót`. Wzorce typu porażka, status `otwarty`, z dowodami z więcej niż dwóch sesji: kod-referencyjny-planu-nigdy-nie-uruchomiony (7) → `/evolve-skill superpowers:writing-plans`; windows-path-w-literale-skryptu (9), cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (6), twierdzenie-o-pliku-bez-odczytu (5) i zweryfikowana-liczba-powtarzana-jako-stala (4) dotyczą zachowania ogólnego, nie pliku konkretnego skilla — ich miejscem jest `~/.claude/CLAUDE.md`, nie `/evolve-skill`. Oba dzisiejsze wzorce mają po jednym dowodzie.
## 2026-09-11 — sesja session_01Ed5FeuzWuuXgE2NexUX3pe (ściąga klawiaturowa w pipelinie wideo, repo Baza wiedzy)
- Założono: pytania-niezalezne-zadawane-po-jednym-na-ture (porażka, skill `superpowers:brainstorming`, status `otwarty`, 1 dowód). Trzy decyzje projektowe — kto składa sekcję, co do niej wchodzi, kiedy powstaje — poszły w trzech osobnych turach, bo skill mówi wprost „Only one question per message". Żadna nie zmieniała pozostałych: opcje drugiego i trzeciego pytania były gotowe przed odpowiedzią na pierwsze. `AskUserQuestion` przyjmuje cztery pytania naraz, a wiki repo Baza wiedzy ma wzorzec sukcesu o łączeniu bramek w jedno pytanie — preferencja użytkownika idzie w drugą stronę niż instrukcja skilla.
- Dopisano dowód: skrypt-z-asercja-zamiast-serii-edycji (ósmy). Pięć wstawek do pięciu plików bez narzędzia `Edit`: `awk` po numerze linii i `python` z asercją `s.count(anchor) == 1`. Nowy wariant wart zapamiętania: trzy bloki do wklejenia w ściągę nie były przepisywane ręcznie, tylko wycięte z `artykul.md` przez `sed -n` w tym samym skrypcie, który składa plik — zgodność cytatu ze źródłem wychodzi wtedy z konstrukcji, nie z porównania po fakcie.
- Dopisano dowód: cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (siódmy, piąta sesja z rzędu). Dwa „Environment update" i dwa „Shell cwd was reset", podpowłoka `( cd … && … )` znowu nie użyta ani razu. Szkody nie było tylko dlatego, że każde kolejne wywołanie zaczynało się od własnego `cd` ze ścieżką bezwzględną — czyli od obejścia, nie od poprawki.
- Wzorce repo (`<repo>/.claude/wiki/`) dotknięte w tej sesji: nowy poprawka-frazy-tylko-w-scenariuszu-zostawia-artykul-i-konspekt (`kurs-uwagi`, `kurs-redakcja`, `kurs-lekcja`) i trzeci dowód do rozjazd-scenariusza-z-konspektem-wykrywany-maszynowo.
- Sygnał globalny: żaden wzorzec globalny nie ma dziś statusu `nawrót`. Wzorce typu porażka, status `otwarty`, z dowodami z więcej niż dwóch sesji bez zmian wobec poprzedniej sesji: kod-referencyjny-planu-nigdy-nie-uruchomiony (7) → `/evolve-skill superpowers:writing-plans`; windows-path-w-literale-skryptu (9), cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (7), twierdzenie-o-pliku-bez-odczytu (5) i zweryfikowana-liczba-powtarzana-jako-stala (4) dotyczą zachowania ogólnego, więc ich miejscem jest `~/.claude/CLAUDE.md`, nie `/evolve-skill`. Dzisiejszy wzorzec ma jeden dowód.

## 2026-09-11 — sesja session_01YVYBimtiCfF3SS1QHH4Cvi (redakcja M02L01 i zmiana reguły rozjazdów, repo Baza wiedzy)
- Założono: wyjatek-dopisany-do-bezwarunkowej-reguly-uzytkownika (porażka, ogólny, status `otwarty`, 1 dowód). Rafał podał regułę bezwarunkowo („rozjazdy zawsze powinny być naprawiane w kurs-redakcja"), a ja wpisałem ją do `redakcja.md` i `SKILL.md` z wyjątkiem, którego w jego zdaniu nie było — i który pokrywał się dokładnie z przypadkiem, o jakim turę wcześniej sam pisałem, że „naprawa to przepisanie tezy całego kroku". Uchylone jedną wiadomością („napraw też rozjazd A"), po której cztery miejsca w skillu trzeba było przepisać drugi raz. Przekładając decyzję użytkownika na tekst dokumentu mam swobodę redakcyjną, której nie mam w odpowiedzi, a wyjątek działa na wszystkie przyszłe sesje.
- Założono: git-diff-bez-head-gubi-wlasna-prace (porażka, ogólny, status `otwarty`, 1 dowód). `git diff --stat` przestał pokazywać właśnie zmienione pliki lekcji, bo Rafał zastagował je równolegle; pierwsza hipoteza brzmiała „ktoś zacommitował", `git log` jej nie potwierdził i dopiero `git status --short` pokazał `M ` w kolumnie indeksu. `git diff` porównuje drzewo z indeksem, a w repo, gdzie commituje użytkownik, indeks jest stanem zewnętrznym.
- Dopisano dowód: skrypt-z-asercja-zamiast-serii-edycji (dziewiąty). Cztery skrypty sesji (10 podmian rozjazdów w artykule, 6 miejsc reguły w dwóch plikach, 5 miejsc przy jej zacieśnianiu, wstawka do dziennika), każdy z asercją „dokładnie jedno trafienie" i przerwaniem całego przebiegu przy innej liczbie; wzorce pisane zwykłymi spacjami, ale dopuszczające twardą, bo treść kursu jest nimi naszpikowana. Żadna podmiana nie chybiła.
- Dopisano dowód: regex-przez-node-e-traci-ukosniki (drugi). Skrypt escapujący wzorzec, przepuszczony przez `node -e` w Bashu, dał `SyntaxError: Invalid regular expression: missing /` — tym razem awaria głośna, nie cicha. Naprawa zgodna ze stroną: ten sam kod w pliku `.mjs` przeszedł za pierwszym razem, a cztery kolejne skrypty tej sesji powstały już wyłącznie jako pliki.
- Wzorce repo (`<repo>/.claude/wiki/`) dotknięte w tej sesji: nowy snapshot-nietrackowanych-plikow-przed-dispatchem (`kurs-redakcja`); drugi dowód i zmiana statusu na `zaadresowany` w agent-redakcji-naprawia-rozjazd-zamiast-go-zglosic; drugie dowody w regex-walidatora-uzyty-jako-fixer-typografii i poprawka-frazy-tylko-w-scenariuszu-zostawia-artykul-i-konspekt.
- Odnotowane, nie zapisane jako wzorzec: bramka wyboru modelu i effortu plus potwierdzenie startu zadziałały jak w `bramki-wyboru-i-potwierdzenia-w-jednym-pytaniu` — jedno `AskUserQuestion` z dwoma pytaniami, po nim plan z konsekwencjami i czekanie na „potwierdzam". To trzymanie się istniejącego wzorca repo, nie nowa obserwacja.
- Sygnał globalny: żaden wzorzec globalny nie ma dziś statusu `nawrót`. Dwa dzisiejsze mają po jednym dowodzie. Wzorce typu porażka, status `otwarty`, z dowodami z więcej niż dwóch sesji — bez zmian wobec poprzedniej sesji.

## 2026-09-11 — sesja session_01YVYBimtiCfF3SS1QHH4Cvi (druga część dnia, po /clear: pamięć rozmowy w M02L01, repo Baza wiedzy)
- Założono: zgloszenie-faktu-wziete-za-wybor-srodka (porażka, ogólny, status `otwarty`, 1 dowód). Rafał zgłosił fakt („w segmencie 8 agent nie wywołał skrzynki, tylko zabrał dane z simple memory"), a ja wybrałem środek zaradczy — restart sesji czatu, ten sam co w segmencie 9 — i wdrożyłem go w czterech plikach lekcji plus przegenerowanym planie. Odpowiedź: „W segmencie 8 nie zmieniamy chatu", całość do cofnięcia. Pytanie `AskUserQuestion` z czterema środkami, zadane dopiero po cofnięciu, rozstrzygnęło sprawę w jednej turze. Zgłoszenie faktu nie jest wskazaniem rozwiązania, a przy treści dydaktycznej środków jest kilka i wszystkie usuwają zgłoszony fakt.
- Dopisano dowód: python-na-windows-drukuje-w-cp1252 (trzeci, trzecia sesja). Dwa razy w jednej sesji; groźniejszy przypadek to skrypt cofający zmiany w segmencie 8, który zapisał wszystkie cztery pliki i padł dopiero na końcowym `print` — exit 1 przy komplecie wykonanej pracy. Powtórzenie skryptu przerwałoby się na asercjach `find()`, bo starych brzmień już nie ma.
- Dopisano dowód: powershell-semantyka-wymaga-sondy-nie-czytania (szósty). `$_` w bloku `catch` wewnątrz `ForEach-Object` to ErrorRecord, nie element potoku: sonda zablokowanych plików zwróciła poprawną liczbę wyników i same puste nazwy (`Zablokowane pliki: , , , …`). Rozstrzygnięte drugą sondą z `$n = $_.Name` przed `try`, nie lekturą dokumentacji.
- Dopisano dowód: cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (ósmy, siódma sesja, piąty raz z rzędu `tools/course-pipeline`). Dziesięć „Environment update" i dwa „Shell cwd was reset"; podpowłoka `( cd … && … )` znowu nie użyta ani razu, mimo że strona była w tej sesji czytana. Raz otarło się o szkodę — skrypt dostał względną ścieżkę `artykul.md` i trafił przypadkiem.
- Wzorce repo (`<repo>/.claude/wiki/`) dotknięte w tej sesji: nowe uwaga-podana-w-rozmowie-omija-bramke-znacznikow (`kurs-uwagi`) i paczka-lektora-kasowana-w-calosci-blokuje-render (`kurs-video`); trzeci dowód w poprawka-frazy-tylko-w-scenariuszu-zostawia-artykul-i-konspekt (bez nawrotu — reguła z 2026-09-11 zadziałała), czwarty w rozjazd-scenariusza-z-konspektem-wykrywany-maszynowo, trzeci w regex-walidatora-uzyty-jako-fixer-typografii.
- Sygnał globalny: żaden wzorzec globalny nie ma dziś statusu `nawrót`. Typu porażka, status `otwarty`, z dowodami z więcej niż dwóch sesji: windows-path-w-literale-skryptu (9), cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (8), kod-referencyjny-planu-nigdy-nie-uruchomiony (7) → `/evolve-skill superpowers:writing-plans`, twierdzenie-o-pliku-bez-odczytu (5), zweryfikowana-liczba-powtarzana-jako-stala (4), subagent-odmawia-commita-mimo-jawnego-wyjatku (3) → `/evolve-skill superpowers:subagent-driven-development`, python-na-windows-drukuje-w-cp1252 (3, awans z dzisiejszym dowodem). Poza dwoma wskazanymi skillami reszta dotyczy zachowania ogólnego, więc ich miejscem jest `~/.claude/CLAUDE.md`, nie `/evolve-skill`. Dzisiejszy nowy wzorzec ma jeden dowód.

## 2026-09-14 — sesja session_01EBknRAiTAR3Phdk3gLiwNP (lekcja 2.2 kursu agenty-ai, repo Baza wiedzy)
- Założono: zakres-linii-wyliczony-z-nienumerowanego-zrzutu (porażka, ogólny, status `otwarty`, 1 dowód). Dwa chybione zakresy w jednej sesji, oba w `konspekt-nagrania.md`, oba z numerów policzonych z `sed -n '1,40p'` zamiast z `cat -n`. Pierwszy zjadł linię sprzed zakresu i zostawił osieroconą za nim; drugi zniszczył półtora kroku konspektu. Skrypt w obu przypadkach wypisał "OK". Pozostałe podmiany sesji, robione z numerowanego wyjścia i z asercją na pierwszej linii, przeszły bez chybienia — asercja na obu granicach złapałaby też te dwie.
- Dopisano dowód: python-na-windows-drukuje-w-cp1252 (czwarty, czwarta sesja). `repr()` linii artykułu padł na znaku "ź"; skrypt nic jeszcze nie zapisywał, więc koszt zerowy, ale diagnostyka wyglądała na pustą. Wszystkie kolejne wywołania sesji z `PYTHONIOENCODING=utf-8`. Problemem zostaje to, że pierwsze wywołanie w sesji regularnie idzie bez prefiksu.
- Dopisano dowód: cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (dziewiąty, ósma sesja). Osiem "Environment update", zero użyć podpowłoki `( cd ... && ... )`. Szkody nie było, bo ścieżki w skryptach były bezwzględne, ale dwa razy musiałem sprawdzić, gdzie stoję, zanim napisałem kolejną komendę.
- Wzorce repo (`<repo>/.claude/wiki/`) dotknięte w tej sesji: nowe agent-czyta-plik-redagowany-przez-agenta-rownoleglego (`kurs-redakcja`) i numeracja-krokow-konspektu-nie-propaguje-sie-na-sciage (`kurs-uwagi`, `kurs-redakcja`); piąty dowód w rozjazd-scenariusza-z-konspektem-wykrywany-maszynowo, drugie dowody w zrzut-ekranu-w-tresci-uwagi i snapshot-nietrackowanych-plikow-przed-dispatchem.
- Sygnał globalny: żaden wzorzec globalny nie ma dziś statusu `nawrót`. Typu porażka, status `otwarty`, z dowodami z więcej niż dwóch sesji: windows-path-w-literale-skryptu (9), cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (9, awans z dzisiejszym dowodem), kod-referencyjny-planu-nigdy-nie-uruchomiony (7) → `/evolve-skill superpowers:writing-plans`, twierdzenie-o-pliku-bez-odczytu (5), zweryfikowana-liczba-powtarzana-jako-stala (4), python-na-windows-drukuje-w-cp1252 (4), subagent-odmawia-commita-mimo-jawnego-wyjatku (3) → `/evolve-skill superpowers:subagent-driven-development`. Dzisiejszy nowy wzorzec ma jeden dowód. Reszta dotyczy zachowania ogólnego, więc ich miejscem jest `~/.claude/CLAUDE.md`, nie `/evolve-skill`.

## 2026-09-15 — sesja session_01FP9aoLo5yuoCv411Gis6gR (design i plan toru stacjonarnego course-pipeline; repo Baza wiedzy)
- Założono: plan-pisany-jednym-zapisem-przekracza-limit-odpowiedzi (porażka, superpowers:writing-plans, status `otwarty`, 1 dowód). Plan 17 zadań / 4167 linii; pierwsza tura z `Write` całości padła na limicie długości odpowiedzi, reszta w siedmiu częściach przez znacznik `<!-- CONTINUE -->` i `Edit`. Bez utraty treści, jedna zmarnowana tura.
- Założono: nazwa-modulu-w-specu-koliduje-z-istniejacym-eksportem (porażka, superpowers:brainstorming, status `otwarty`, 1 dowód). Spec wprowadził `konspekt.js`/`parseKonspekt`, a `plan-nagrania.js` eksportuje `parseKonspekt` od 2026-09-09; wyszło przy czytaniu eksportów do planu, spec przepisany na `konspekt-bloku.js`/`parseBlockKonspekt` przed użyciem nazwy w planie.
- Dopisano dowód: kod-referencyjny-planu-nigdy-nie-uruchomiony (ósmy, ósma sesja). Plan z pięcioma modułami i ok. 40 testami oddany po lekturze własnej i codexa; żaden blok nie uruchomiony.
- Dopisano dowód: cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (dziesiąty, dziewiąta sesja). Trzy „Environment update", dwa „Shell cwd was reset", podpowłoka `( cd … && … )` nie użyta ani razu.
- Dopisano dowód przeciwny: pdf-read-wymaga-pdftotext — `Read` przeczytał trzystronicowy PDF programu kursu z tekstem i obrazami stron, bez błędu o `pdftoppm`. Przesłanka wzorca nie potwierdziła się; strona zostaje otwarta z adnotacją „jeden dowód za, jeden przeciw".
- Odnotowane, nie zapisane jako wzorzec: `grep -n -i "TBD\|TODO\|\?\?\?"` w Git Bash dopasował każdą linię pliku, bo w BRE `\?` to kwantyfikator „opcjonalny", nie literalny znak zapytania; naprawione przez `grep -F -e`. Jedna sesja, jeden przypadek.
- Wzorce repo (`<repo>/.claude/wiki/`) dotknięte w tej sesji: żadne — sesja nie uruchomiła żadnego skilla `kurs-*`, tylko projektowała zmiany w nich (log repo ma wpis „Bez nowych wzorców" z odnotowaną obserwacją o prezentacji nowego skilla w rodzinie).
- Sygnał globalny: żaden wzorzec globalny nie ma dziś statusu `nawrót`. Typu porażka, status `otwarty`, z dowodami z więcej niż dwóch sesji: cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (10), windows-path-w-literale-skryptu (9), kod-referencyjny-planu-nigdy-nie-uruchomiony (8) → `/evolve-skill superpowers:writing-plans`, twierdzenie-o-pliku-bez-odczytu (5), zweryfikowana-liczba-powtarzana-jako-stala (4), python-na-windows-drukuje-w-cp1252 (4), subagent-odmawia-commita-mimo-jawnego-wyjatku (3) → `/evolve-skill superpowers:subagent-driven-development`. Oba dzisiejsze nowe wzorce mają po jednym dowodzie. Uwaga z logu 2026-09-11 nadal aktualna: skille `superpowers:*` leżą w cache pluginu, więc `/evolve-skill` na nich kończy się odmową — droga prowadzi przez własną kopię w `~/.claude/skills/`.

## 2026-09-16 — sesja (id niedostępny) (konsolidacja artefaktów toru B wideo: spec + plan; repo Baza wiedzy)
- Założono: timeout-pytania-podpowiada-kontynuacje (sukces, ogólny, status `otwarty`, 1 dowód). `AskUserQuestion` po 60 s zwraca wynik narzędzia z instrukcją „Proceed using your best judgment based on the context so far", która wprost zaprzecza bezwarunkowej regule z `~/.claude/CLAUDE.md` („Timeout pytania to nie zgoda"). Dwa razy w jednej sesji, oba razy zignorowana poprawnie: przy wyborze trybu egzekucji planu i przy zgodzie na pracę wprost na `main`. Ten drugi przypadek jest mocniejszy, bo „best judgment" złamałoby też wymaganie samego skilla `subagent-driven-development` („Never start implementation on a main/master branch without your human partner's explicit consent"). Przyczyna źródłowa: podpowiedź przychodzi kanałem wyniku narzędzia, czyli tam, gdzie normalnie stoją fakty, a nie polecenia, i jest sformułowana jako uprawnienie, nie jako pytanie — więc nie uruchamia odruchu rozstrzygania konfliktu. Rozwiązanie zapisane z trzema częściami odpowiedzi, w tym z zakazem ponawiania pytania kolejnym wywołaniem narzędzia (odpala następny 60-sekundowy timeout i zapętla bramkę).
- Dopisano dowód: brief-nie-wymienia-testow-ktore-zmiana-uniewaznia (drugi, druga sesja). Plan dodawał bramkę odrzucającą lekcję bez sekcji „Do wklejenia"; self-review planu tego nie złapał, złapał codex — dwiema uwagami naraz. `lekcjaDemo()` w `tests/plan-nagrania.test.js:648` domyślnie buduje lekcję z fixture'u `KONSPEKT_DEMO`, którego nowa bramka odrzuca, a `tests/validate-lesson.test.js:354,365,379` zapisuje ten sam fixture, przy czym dwa z tych testów wołają `generateRecordingPlan`. Wzorzec wzmocniony o wariant, którego pierwotny dowód nie obejmował: najdroższa nie jest asercja o zmienianym zachowaniu, tylko **fixture, którego nowa walidacja nie przepuszcza** — unieważnia wszystkie testy, które go dotykają, także te o zmienianym zachowaniu milczące, i nie znajdzie go `grep` po nazwie funkcji ani po komunikacie błędu.
- Dopisano dowód: kod-referencyjny-planu-nigdy-nie-uruchomiony (dziewiąty, dziewiąta sesja). 8 zadań, 46 kroków, komplet fragmentów `plan-nagrania.js` i `validate-lesson.js` plus ok. 15 bloków testowych — żaden nie uruchomiony. Tym razem koszt policzalny: oba defekty znalezione przez codexa pokazałby natychmiast `npx vitest run`. Baseline suite'u w tej samej sesji uruchomiłem (121/121), więc dostęp do wykonania był, tylko nieużyty na kodzie referencyjnym. Do „Rozwiązania" dopisany tańszy wariant: przepuścić sam nowy fixture przez istniejący suite, bo to łapie dokładnie tę klasę defektu.
- Dopisano dowód: sdd-bez-commitow-paczki-review-z-migawek (czwarty). Pierwszy raz prewencyjnie: ruling o migawkach zapisany w ledgerze w pre-flight scanie, zanim cokolwiek ruszyło, wraz z kontrolą `git status --short` przed i po zadaniu na wypadek plików, których plan nie wymienia. Egzekucja nie ruszyła (bramka zgody), więc to dowód na wzorzec jako regułę pre-flight, nie na samo wykonanie.
- Odnotowane, nie zapisane jako wzorzec: `subagent-driven-development` wymaga jawnej zgody na pracę na `main` i domyślnie chce worktree, co w tym repo (Rafał sam commituje, praca w drzewie roboczym) jest bramką na starcie każdego runu. Istnieje już siostrzany wzorzec dla skilla zamykającego (`branch-finish-zaklada-oddzielna-galaz-feature`); to byłby ten sam problem w skillu otwierającym, ale mam go z jednej sesji.
- Odnotowane, nie zapisane jako wzorzec: nadmiarowe escapowanie backticków (`\\`` zamiast `\``) przy pisaniu literału szablonowego JS wewnątrz bloku kodu markdown w planie; złapane własną kontrolą `cat -A`. Jedna sesja.
- Wzorce repo (`<repo>/.claude/wiki/`) dotknięte w tej sesji: drugi dowód w numeracja-krokow-konspektu-nie-propaguje-sie-na-sciage, z korektą jego „Rozwiazania" — spec i plan z tej sesji scalają dwa pliki w jeden, ale kontroli numeracji nie kasują.
- Sygnał globalny: żaden wzorzec globalny nie ma dziś statusu `nawrót`. Typu porażka, status `otwarty`, z dowodami z więcej niż dwóch sesji: cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (10), kod-referencyjny-planu-nigdy-nie-uruchomiony (9, awans z dzisiejszym dowodem) → `/evolve-skill superpowers:writing-plans`, windows-path-w-literale-skryptu (9), twierdzenie-o-pliku-bez-odczytu (5), zweryfikowana-liczba-powtarzana-jako-stala (4), python-na-windows-drukuje-w-cp1252 (4), subagent-odmawia-commita-mimo-jawnego-wyjatku (3) → `/evolve-skill superpowers:subagent-driven-development`. `brief-nie-wymienia-testow-ktore-zmiana-uniewaznia` ma teraz 2 dowody z 2 sesji — jedna sesja do progu. Dzisiejszy nowy wzorzec ma jeden dowód i jest typu sukces, więc sygnału nie generuje. Uwaga z logów 2026-09-11 i 2026-09-15 nadal aktualna: skille `superpowers:*` leżą w cache pluginu, więc `/evolve-skill` na nich kończy się odmową — droga prowadzi przez własną kopię w `~/.claude/skills/`.

## 2026-09-16 — sesja (id niedostępny) (markery źródeł, tytuły modułu 2, aktywność wideo; repo Baza wiedzy + code-busters-v2)
- Założono: odpowiedz-spoza-opcji-rozstrzygana-dopytaniem (sukces, ogólny, status `otwarty`, 1 dowód). Na pytanie „który status wpuszcza wideo na platformę?" (trzy opcje o mechanizmie bramki) padła odpowiedź wolnym tekstem „kurs-publikuj " — nazwa skilla publikującego, czyli byt z innej osi. Pozostałe trzy odpowiedzi z tego samego wywołania były jednoznaczne i zostały wzięte bez pytania; to jedno ponowione z trzema odczytaniami, wybór „1". Gdyby zmapować je samodzielnie na „bramkę statusu", powstałby kod blokujący publikację wideo, której nikt nie chciał blokować.
- Założono: kontrakt-miedzy-repo-sprawdzony-realnym-payloadem (sukces, ogólny, status `otwarty`, 1 dowód). Nowa kategoria `VIDEO` dotknęła dwóch repo: payload składa `tools/course-pipeline/src/publish.js`, waliduje zod `apps/cms/src/utils/coursePipeline/schema.ts`. Przed oddaniem: skrypt w scratchpadzie zbudował payload modułu 2 prawdziwym `buildLessonPayload` (13 aktywności, ID wstrzyknięte w pamięci, pliki lekcji nietknięte), tymczasowy test w repo platformy przepuścił ten JSON przez `lessonPayloadSchema` — zielony, jedna aktywność `VIDEO` — po czym test skasowany i `git status` platformy sprawdzony. Same testy jednostkowe (801 tu, 73 tam) tego nie pokrywały: obie strony przeszłyby też przy `videoUrl` wysyłanym liczbą zamiast tekstem.
- Dopisano dowód: timeout-pytania-podpowiada-kontynuacje (drugi, druga sesja). Tym razem timeout padł na pytaniu doprecyzowującym, gdzie stawką był kształt kodu, nie bramka skilla — podpowiedź „Proceed using your best judgment" znów zignorowana, odpowiedź złożona z trzech części zapisanych w „Rozwiązaniu" wzorca, użytkownik wrócił wiadomością „1".
- Odnotowane, nie zapisane jako wzorzec: dołożenie jednej pozycji do planu publikacji (wiersz `video: skip`, drukowany także dla lekcji bez wideo) zerwało cztery istniejące testy planu naraz — każdy nowy artefakt w planie jest zmianą kontraktu testów, nie tylko dodatkiem do wyjścia. Jedna sesja; siostrzany wzorzec `brief-nie-wymienia-testow-ktore-zmiana-uniewaznia` dotyczy briefów, nie własnej pracy inline.
- Wzorce repo (`<repo>/.claude/wiki/`) dotknięte w tej sesji: szósty dowód w grep-po-tresciach-kursu-gubi-twarde-spacje-i-emoji — pierwszy spoza rodziny `kurs-*`, `Edit` odbity trzy razy o U+00A0 w nagłówkach artykułów przy pracy nad publikacją.
- Sygnał globalny: żaden wzorzec globalny nie ma dziś statusu `nawrót`. Oba dzisiejsze nowe wzorce są typu sukces, więc sygnału nie generują. Bez zmian względem 2026-09-16 rano: cd-w-komendzie-bash-przestawia-katalog-kolejnych-wywolan (10), kod-referencyjny-planu-nigdy-nie-uruchomiony (9) → `/evolve-skill superpowers:writing-plans`, windows-path-w-literale-skryptu (9), twierdzenie-o-pliku-bez-odczytu (5), zweryfikowana-liczba-powtarzana-jako-stala (4), python-na-windows-drukuje-w-cp1252 (4), subagent-odmawia-commita-mimo-jawnego-wyjatku (3) → `/evolve-skill superpowers:subagent-driven-development`.
